// ─────────────────────────────────────────────────────────────────────────────
// SHELVED — deliberately dormant, imported by nothing (founder call,
// 2026-07-15, modules cutover). Do NOT delete and do NOT re-wire without a
// founder decision.
//
// "Bring a situation" was the core-loop entry door: the user arrives with
// something live — a conflict, a reaction, a moment — precisely because they
// can't yet see what it's about, and the conversation reveals it. The modules
// cutover replaced the fixed doors with founder-authored modules (every
// conversation now starts inside a module), so this door has no live surface.
//
// It is shelved, not dead: the founder's stated intent is a FUTURE PRODUCT
// that brings the open "situation" entrance back on top of the module
// structure — leveraging the Manual and modules as accumulated context for
// navigating a live situation. When that product is scoped, this opener (and
// the entry posture it encodes: scene-first, not topic-first) is the seed.
//
// Same treatment as the four ND persona-delta files: kept by settled
// decision, skipped by /overbuild-check.
// ─────────────────────────────────────────────────────────────────────────────

// The fixed opener for the shelved Situation entrance. Server-emitted as
// turn 1, verbatim, with no model call — a fixed opener can't drift the way
// a model-generated one does (the "what's on your mind today?" drift that
// made every session start on a topic, not a scene).
export const SITUATION_OPENER =
  "What situation is top of mind for you right now that would be helpful to better understand.";
