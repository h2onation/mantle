# state.md — What's True Right Now

> **Authority level**: Volatile. This is the only doc that changes weekly.  
> **Audience**: You (to know what's live and what's broken) and Claude Code agents (to avoid building on broken foundations or duplicating what exists).  
> **Maintenance rule**: Update this doc before every merge to main. Use `/ship` or update manually. If a section's "last verified" date is more than 7 days old, treat it as suspect.  
> **Related docs**: system.md covers how the architecture works. intent.md covers what we're building and why. rules.md covers what never changes.

---

## Deployed Features
*Last verified: 2026-05-14*

- Codebase-wide simplification — passes 1 + 2 (2026-05-14, tender-allen worktree): Two cleanup passes plus follow-up batches, focused on dedupe and removing dead state without changing user-facing behavior. **New helpers.** `extractResponseText(response)` in `src/lib/anthropic.ts` (replaces 4 inline `response.content[0].type === "text" ? ...` repetitions); `requireAdmin()` in `src/lib/admin/verify-admin.ts` (admin guard returning client + userId or Response — sibling to the later-shipped `requireUser`); `listAllAuthUsers(admin)` in `src/lib/admin/list-auth-users.ts` (centralizes `listUsers({perPage:1000})` + emailMap construction across 4 admin routes that hand-rolled it); `isValidEmail(email)` in `src/lib/beta-allowlist.ts` (replaces duplicated regex in waitlist + admin/beta-allowlist); `firstNameOrNull(name)` in `src/lib/utils/name.ts` (sibling to `firstNameFrom`; null fallback for linq sites that decide whether to address by name); `useAsyncFetch<T>(url)` in `src/lib/hooks/useAsyncFetch.ts` (data-side dedup for 4 admin health panels); `renderManualEntryFull(entry)` in `src/lib/manual/layers.ts` (canonical "Manual entry in a prompt" rendering). **Centralized constants.** 5 model IDs (`PERSONA_MODEL`, `EXTRACTION_MODEL`, `COMPOSITION_MODEL`, `SUMMARY_MODEL`, `SIMULATION_MODEL`) moved into `src/lib/persona/config.ts`; `CHECKPOINT_ACTIONS` map (system message + natural reply paired) replaces stringly-typed if/else in `mapSystemMessages`; `ConversationMode` + `CONVERSATION_MODES` tuple, `CheckpointAction` type, `PersonaMode` import replace duplicate inline literals across 6+ files; `LAYER_ROMAN` constant moved from EmptyLayer + PopulatedLayer into `layer-definitions.ts`. **Migrations.** 13 admin routes to `requireAdmin()`; 4 admin routes to `listAllAuthUsers()`; 4 admin health panels to `useAsyncFetch`; voice rule "Jove" hardcoded → `PERSONA_NAME` templated in 4 voice files + voice-scaffold (will invalidate Anthropic prompt cache once on next request); stale "Sage" comments swept to "Jove" across 8 files. **Perf wins.** `createAdminClient()` module-scope cached (saves Supabase constructor on every API route + webhook on warm Edge instances); `mentionsPersona` regex hoisted to module load (was recompiling per call on group-message hot path); `Promise.all` in `checkpoint/confirm/route.ts` (msg + conv lookups now parallel, with sanity check that conv.id matches msg.conversation_id) and in `conversations/route.ts` (latest/count/firstText queries); `useMemo` for `refinementCeilingActive` (MobileSession) and `buildLayers` (MobileManual); MobileSettings phone fetch deferred until Settings tab visited (no longer fires on app load for users who never visit Settings); `manual_viewed` analytics fires once per tab visit instead of duplicating after every checkpoint confirm; `conversations/route.ts` count-only query no longer reads message content (drops payload for power users with long text-channel histories). **Dead code removed.** `hasManualContext` flag (redundant with `manualComponents.length > 0`); `modalStateLoading` state (admitted-unused); `LEAD_IN_LINE` dead ternary; `ManualEntryForContext` interface collapsed to `ManualEntry` alias; unused `getLayer()` helper; `{true && (...)}` wrappers in MobileSettings (3 sites); duplicate `ROMAN` constant. **Phone normalization.** Added `@deprecated` JSDoc on `normalizePhone` pointing future code at `normalizeUSToE164` — documents the divergence (always-prefixed vs null-on-invalid) without touching the 11 linq/messaging call sites that depend on the always-prefixed semantic. Full migration deferred (HIGH risk; see `docs/deferred-refactors.md`). **New documentation.** `docs/deferred-refactors.md`: register of items considered but explicitly deferred during the simplification passes, with value/risk/trigger for each. Includes the items the parallel sessions also deferred (small follow-ups to `guided-intake-copy.test.ts`, `buildSystemPrompt` wrapper collapse) so future-you has one place to find the open work. **Merge integration.** Pass 1 + 2 work was authored on a worktree based on a week-old main; merge integrated cleanly with the parallel SSE / BuildPromptOptions / buildTier3 / requireUser / prompt-caching sessions that landed independently. 7 conflicts resolved (mostly accepting main's structural restructures and layering pass-1's dedupe on top); zero behavior conflicts. 730 tests pass, 8 pre-existing TS errors unchanged, build clean. Net feature-branch delta: 596 insertions / 697 deletions (−101 LOC) across 80 files.
- Anthropic prompt caching on chat + extraction system prompts (2026-05-14, vigilant-johnson worktree): The Jove system prompt is now sent to Anthropic as a three-block `system` array with a `cache_control: { type: "ephemeral" }` marker on the largest stable block. **Block layout.** `tier1` = intro + Tier 1 constitutional rules (smallest, never changes). `staticContext` = Tier 2 voice + compressed older Manual entries (stable per session unless persona modes change or a new entry is confirmed; carries the marker). `dynamic` = Tier 3 mechanics + current-session Manual entries + session/extraction/transcript/exploration context (changes every turn, never cached). **New API.** `buildSystemPromptBlocks(opts: OneOnOnePromptOptions)` in `src/lib/persona/system-prompt.ts` returns `{ tier1, staticContext, dynamic }`. The group-chat path keeps using `buildGroupPrompt` directly (no caching — group sessions are too short and varied for the 5-minute window). Legacy `buildSystemPrompt` (now narrowed by `options.kind === "group"`) is unchanged for admin renderer + Linq bridges + simulate-user + all other non-hot-path callers. `prepareManualContextBlocks(entries, currentConversationId)` in `src/lib/persona/manual-context.ts` returns `{ recent, older }`; the legacy `prepareManualContext` wraps it and reproduces byte-identical output. **Wired surfaces.** `src/lib/persona/call-persona.ts` (chat hot path, streaming) constructs `SystemBlock[]` with one cache_control marker; `src/lib/persona/extraction.ts` (per-turn classifier) does the same on the EXTRACTION_SYSTEM constant. Confirm-checkpoint, generate-summary, simulate-user, group-bridge, and persona-bridge are intentionally NOT cached (sparse calls / dev-only / deprecating). **anthropic.ts changes.** `AnthropicRequest.system` accepts `string | SystemBlock[]`; `AnthropicResponse.usage` exposes `cache_creation_input_tokens` + `cache_read_input_tokens` + `input_tokens` + `output_tokens`. New `parseStreamUsage(event)` extracts token counts from streaming `message_start` + `message_delta` SSE events. **SSE plumbing.** `parseAnthropicStream` in `src/lib/anthropic-sse.ts` gained an optional `onUsage` callback — call-persona uses it to capture token counts inside the shared parser loop rather than re-implementing event parsing. No `anthropic-beta` header needed — prompt caching is GA. **Observability.** New `cache_performance` event in `src/lib/observability/log.ts` carries surface (`chat` | `extraction`), model, and all four token fields. Logged once per chat stream after the response completes and once per extraction call. **Live verification (2026-05-14, devtest@test.com, two consecutive chat turns within ~30s).** Chat turn 1: `cache_read=0 cache_create=3493 uncached_input=4459` (miss, prefix cached). Chat turn 2: `cache_read=3493 cache_create=4502 uncached_input=15` (HIT — 3493 tokens served from cache, ~91% of input now cached or near-zero). Extraction turn 1: `cache_read=0 cache_create=3931 uncached_input=2913` (miss). Extraction turn 2: `cache_read=3931 cache_create=0 uncached_input=2971` (clean HIT — no new cache write needed). **Cost math.** At Sonnet's published rates and a typical 10-turn session, the chat path saves ~9 × 3500 × 0.90 = ~28,400 cached tokens × ~$3/M = ~$0.085 per session; extraction saves ~9 × 3931 × 0.90 = ~31,800 cached tokens per session. Roughly 70-80% reduction in chat+extraction input cost across the active portion of a session. **Tests.** +31 tests (730 total post-merge). New: stability test (`buildSystemPromptBlocks` byte-identical across calls with same inputs), block-separation tests (Tier 3 NOT in staticContext, recent entries NOT in staticContext, older entries IN staticContext, identical staticContext across turnCount + checkpointApproaching changes), `prepareManualContextBlocks` split tests, source-contract tests asserting array-form system + exactly one cache_control marker in both call-persona.ts and extraction.ts, `parseStreamUsage` tests, `logEvent` cache_performance smoke. Build clean. Files: `src/lib/anthropic.ts`, `src/lib/anthropic.test.ts` (new), `src/lib/anthropic-sse.ts` (onUsage callback), `src/lib/persona/system-prompt.ts`, `src/lib/persona/system-prompt.test.ts`, `src/lib/persona/manual-context.ts`, `src/lib/persona/manual-context.test.ts`, `src/lib/persona/call-persona.ts`, `src/lib/persona/call-persona.test.ts`, `src/lib/persona/extraction.ts`, `src/lib/persona/extraction.test.ts`, `src/lib/observability/log.ts`, `src/lib/observability/log.test.ts`.
- Chat surface composition — Jove/user bubble color swap + Stone input card (2026-05-14, chat-input-stone-card branch): Two coordinated changes to how the chat surface reads. **(1) Bubble color swap.** `Bubble.tsx` exchanges the surface tokens between `JOVE_STYLE` and `USER_STYLE`: Jove now uses `--session-walnut-surface` (the lighter cream / paper tone), user bubbles use `--session-bubble-user` (the warm walnut wash). Reverses the 2026-05-13 "Bubble color flip." Rationale: Jove's voice reads as quiet and spacious — pairs better with the cream / paper surface — while the user's contributions sit on the warmer walnut wash. As a happy side-effect the `--session-bubble-user` token name now correctly matches its consumer color, including the chat input (next change) which keeps that token. Borders swapped to match (`--session-bubble-border` ↔ `--session-bubble-user-border`). Both light and dark mode contrast verified — the sage `JOVE` eyebrow has strong contrast on the cream surface in both themes. **(2) Stone input card.** Replaces the 999px walnut pill + TextBtn "SEND ›" (caps-mono with ink underline) with a soft 21px walnut-cream card and a bare 18px stroke up-arrow glyph at bottom-right. All three button states (mic / stop-recording / send) now share the same 44px transparent hit area (`padding: 13px; margin: -13px`) — iOS HIG preserved while the visible glyph stays at 18px and the row stays tight. The stop-recording state strips its previous 32px dark-circle well to a bare 14px sage rounded square with the existing `voicePulse` animation. Card surface switched from `--session-bubble-user` to `--session-walnut-surface` so the input visually pairs with Jove's bubbles — both read as "paper / writing surface." Border and shadow switched accordingly (`--session-bubble-border`, `--session-bubble-shadow`, lighter than the previous `--session-plate-shadow`). Card grows vertically with the textarea via `align-items: flex-end`, so the send glyph stays anchored bottom-right as the user composes long-form. `min-height: 56px` for a comfortable resting state; `max-height: 240px` caps card growth. Textarea max-height in `useLayoutEffect` bumped from `lineHeight * 3.5` (~91px, 3.5 lines) to `lineHeight * 6` (~156px, 6 lines), past which the textarea scrolls internally so the conversation above stays visible. Padding tightened from `10px 14px` (uniform pill) to `12px 8px 12px 16px`; gap between textarea and button compressed from 10px to 4px. Mobile result on a 375 viewport (343px-wide card): writing area went from 281px to **295px** (+14px); distance from visible glyph to card right edge went from 15px to 9px — comparable to iMessage's tight composition spec. Placeholder font-size 15→16 to match the Spectral italic typing register, repositioned `bottom: 14px` rather than vertically centered so it lands where typed text will sit. **Files.** `src/components/shared/Bubble.tsx` (bubble swap), `src/components/mobile/ChatInput.tsx` (Stone card, button stripping, growth/scroll behaviour, padding tightening). 699 tests pass. Build clean.
- Authentic post-confirm + anti-universal-tone + simulator [END] (2026-05-14, happy-buck worktree): Three discrete fixes from user testing round 3 (the "I Disappear When Nobody Needs Carrying" / "I Spit the Signal Back" case + the simulator acknowledgment-loop). **(1) Post-confirm flow rewritten.** The old "In. A working name: 'X.' Yours to change." stamp + entries-summary line read as admin notifications, not Jove speaking. The title is already in the trigger card, the chat-history label already shows the layer; repeating in Jove's text was redundant. New shape (both first-confirm and subsequent): pinned `Saved.` opener + (first-confirm only) "A Manual takes time to build. Best results come from showing up daily over the next two weeks. You can change the name or sharpen the entry anytime." + a continue-or-pivot offer naming a specific thread from the just-finished conversation. Drops the previous round's forward-question requirement — after a heavy save the user might be tapped out, so the offer gives both paths (continue OR pivot, joined by "or") and lets the user choose. Wiring: dropped the templated Message 1 prepend in `confirm/route.ts`; rewrote both post-confirm `Tier3Block` render functions in the new data-driven pipeline; removed `postConfirmContext` from `BuildPromptOptions`, `Tier3Flags`, and `CallPersonaOptions` (no substitutions needed anymore); Phase B fallback rewritten to match the new shape ("Saved. / [scaffolding for first-confirm] / We could keep going with what we just touched, or pivot to something else if this is enough for now."); typing-indicator condition in `MobileSession` expanded to also fire when the last message is a checkpoint card so the user sees feedback during the post-confirm composition wait. **(2) Body anti-universal-tone rule (composition prompt).** The previous "scope of evidence" rule caught explicit "always / every / all / never" but missed phrasings that read universal without them: "every low-stakes moment," "every friendship," "in those rooms," "whenever this happens," "any time someone X." Now forbidden explicitly with bad → good rewrite examples inline ("In those rooms I'm scanning." → "In moments like that, I notice I'm scanning."). **(3) Headline anti-abstract-verb rule.** The previous round forbade body-as-agent ("Stomach Pushes Me to Fix the Call"); this round adds abstract / metaphorical verbs ("disappear," "vanish," "lose myself," "fade," "go missing," "come undone," "fall apart," "dissolve"). These describe a felt internal state, not an observable behavior. "I Disappear When Nobody Needs Carrying" added to bad examples; "I Steer Toward Problems When Friends Just Want to Chat," "I Get Restless When the Room Has No Job for Me," "I Build Heavy Topics When None Are There" added to good. The headline verb must describe what a friend watching the scene would see the user DO. **(4) Simulator [END] lenient match.** Bonus fix from the same testing — `dev-simulate/route.ts` now uses `userMessage.includes("[END]")` instead of `userMessage.trim() === "[END]"`. Haiku sometimes emits `[END]` at the tail of a longer goodbye ("I will. Thanks. [END]"); the strict equality check missed those and the loop kept running until both sides broke character into an acknowledgment loop ("Standing by." / "👋" / "Ready when you are."). Files: `src/lib/persona/confirm-checkpoint.ts`, `src/lib/persona/system-prompt.ts` (+ test, + tier3 snapshot), `src/lib/persona/call-persona.ts` (+ test), `src/app/api/checkpoint/confirm/route.ts`, `src/components/mobile/MobileSession.tsx`, `src/app/api/dev-simulate/route.ts`. 699 tests pass (was 696; +3 new contract tests, 2 tier3 snapshots regenerated). Build clean.
- `buildTier3` data-driven block pipeline (2026-05-14, relaxed-bose worktree): Pure refactor — replaced the ~350-line if/else ladder in `src/lib/persona/system-prompt.ts` that mutated a string accumulator with a `readonly Tier3Block[]` array of 13 entries (one per conditional or always-on block). Each entry has `{ id, shouldRender, render }`. `buildTier3` is now 6 LOC: filter + map + join. Block order, content, and join behavior preserved exactly so prompt output is byte-identical. The 13 blocks: `first-message`, `guided-intake`, `upload`, `returning-user`, `returning-user-first-turn-situation` (was nested in returning-user), `checkpoints`, `first-checkpoint`, `post-rejection`, `post-confirm-first-message-2`, `post-confirm-subsequent-single`, `adapting-short-answers` (always-on), `readiness-gate`, `clinical-and-tail` (always-on, includes the FIRST SESSION ternary on `isNewUser`). Each block is now independently inspectable and unit-testable. New `src/lib/persona/system-prompt.tier3.test.ts` adds 15 byte-level snapshot tests covering every block's firing combo (one per block in isolation plus a max-fire combo) — these are the safety net that proved byte-identity through the refactor. All 169 existing `system-prompt.test.ts` assertions pass unchanged. 696 tests pass (was 681; +15 new snapshot tests). Build clean. 8 pre-existing TS errors unchanged.
- SSE parser unification + BuildPromptOptions discriminated union (2026-05-14, happy-nobel worktree): Two pure refactors from a follow-up audit. **(1) SSE parser unification.** New `src/lib/anthropic-sse.ts` carries the hand-rolled `getReader()` + `TextDecoder` + buffer-split + `data:` parse loop that previously lived inline in `call-persona.ts`. Single-callback API (`onTextDelta`) — call-persona is the only consumer of Anthropic's UPSTREAM SSE protocol (`content_block_delta`/`text_delta`). Sibling to `parseSSEStream` in `src/lib/utils/sse-parser.ts`, which parses our DOWNSTREAM protocol (`text_delta`/`message_complete`/`error`). Different event vocabularies so they cannot share a parser. As a follow-on, migrated `src/app/api/dev-simulate/route.ts` to consume `parseSSEStream` (it was hand-rolling the same downstream loop). Net: every SSE consumer in the repo now routes through one of the two shared parsers. Streaming cadence preserved by construction (sync callback per delta = sync inline body per delta). **(2) BuildPromptOptions discriminated union.** `BuildPromptOptions` split into `OneOnOnePromptOptions | GroupPromptOptions` on a `kind` discriminator. The 1:1 logic in `buildSystemPrompt` is now type-narrowed after the `kind === "group"` early return — no more implicit `if (groupContext)` check inside the 1:1 destructure. Group caller in `src/lib/linq/group-bridge.ts` drops the seven 1:1-only placeholder fields it was passing. `buildPromptOptionsFromContext` returns `OneOnOnePromptOptions` so call sites pick up `kind: "oneOnOne"` for free via spread. Dropped the now-redundant `hasManualContext` flag from `groupContext` — the `manualComponents.length > 0` check already gates the MANUAL CONTEXT RULES block. Added a comment near the union flagging the `Partial<Union>` distribution gotcha (`Partial<A | B>` doesn't behave like `Partial<A> | Partial<B>` — both `prompt-sections.ts` and the test helper needed narrowing to a specific variant). One follow-up deferred to `docs/deferred-refactors.md`: `guided-intake-copy.test.ts` calls `buildSystemPrompt` with `ConversationContext`-shaped args (2 pre-existing TS errors); should be reconciled to `OneOnOnePromptOptions` shape. 681 tests pass. Build clean.
- `requireUser()` API auth helper (2026-05-14, hardcore-wilbur worktree): Pure refactor — extracted the repeated `createClient()` + `supabase.auth.getUser()` + 401 boilerplate from 16 invocations across 13 authenticated API routes into a single helper at `src/lib/auth/require-user.ts`. Mirrors the prior `verifyAdmin()` simplification pass. Caller pattern: `const auth = await requireUser(); if (auth instanceof Response) return auth; const { user } = auth;`. Two options handle the variants surfaced in the audit: `errorMessage` (lowercase `"unauthenticated"` for `modal-progress` + `onboarding-status`), and `onUnauthorized` callback (sync, fires before the 401 — used by `checkpoint/confirm` to emit its `confirm_outcome` telemetry log). `recordApiError` pattern stayed at the route boundary unchanged — callers capture `user.id` after the auth check. Net **-82 lines** across the API surface. Migrated: `account/delete`, `chat`, `checkpoint/confirm`, `conversations`, `conversations/complete`, `manual`, `modal-progress`, `onboarding-status`, `session/summary`, `settings/link-phone`, `user/phone` (×3), `user/phone/verify`, `voice/token`. Out of scope: `analytics/identity` (returns 200 + null hashedId on no user, not 401 — different shape), `dev-*` (looser auth), `webhooks/*` (signed payload, not user auth). No test changes needed — tests already mocked `@/lib/supabase/server` and the helper goes through that mock transparently. 681 tests pass. Build clean.
- Forming-state UX + post-confirm fallback + headline trigger requirement (2026-05-14, happy-buck worktree): Four follow-up fixes from a second round of user testing against the "I Swallow the Answer Before I Can Say It" checkpoint case (universalized title from one example, lead-in bubble serialized rather than replacing the card, dead-end after save). **(1) Forming state is now a transient typing-indicator label, not a persisted chat bubble.** New `composing` SSE event type emitted from the server right after deterministic detection passes but BEFORE the slow composition Opus call. The client (`useChat.ts`, `MobileSession.tsx`) renders "Something is forming…" inside the existing typing indicator alongside the ❦ glyph during the ~10-15s wait. Cleared automatically when the trigger card's `message_complete` event arrives — the card REPLACES the forming state instead of appearing alongside a separate bubble. The persisted lead-in assistant message ("A pattern came through in what you said.") that used to be inserted with a backdated timestamp is deleted entirely; existing rows in older conversations still render but no new ones are created. **(2) Headlines must name a specific trigger.** Stronger HEADLINE rule in the composition prompt: "Format: 'I [verb] when [specific trigger]' or 'I [verb] before [specific event]' or 'I [verb] after [specific moment].' The trigger names a concrete situation — a person, a moment, a sensation. Not 'before I can say it' (vague — what stops me?) but 'when guilt hits' or 'when someone waits for me to answer' (specific). The title should answer 'WHEN does this fire?' not just 'WHAT happens?'" Plus: if only one example is in evidence, soften with "can" or "sometimes" to avoid over-claiming. "I Swallow the Answer Before I Can Say It" added to bad examples; "I Can Freeze When Asked What I Want" added to good. **(3) Post-confirm Sonnet failures emit a deterministic fallback Message 2.** Previously when the post-confirm scaffolding LLM call failed, we closed the stream silently (preserving the entry's save success but leaving the user staring at Message 1 with no forward question). New behavior: `buildPostConfirmFallback` returns a templated Message 2 mirroring the prompt structure — pinned copy plus a generic forward question ("What's still open in this for you?"). For `first-message-2`: layer-name opener + the pinned "real Manual takes time" paragraph + the question. For `subsequent-single`: stamp line + entries summary + the question. The fallback is saved to DB so reload reads consistently. Sonnet wins when it works; the template wins when it doesn't. **(4) Saved-to label puts layer name first.** Chat-history `Plate` for confirmed checkpoints now renders `"Saved to <Layer Name> — Layer N"` instead of `"Saved to Layer N — <Layer Name>"`. The meaningful name leads ("SAVED TO SOME OF MY PATTERNS — LAYER 1"), the ordinal supports. Files: `src/lib/persona/confirm-checkpoint.ts` (headline), `src/lib/persona/call-persona.ts` (composing event + fallback + lead-in removal), `src/lib/persona/call-persona.test.ts` (fallback contract tests, replacing the old "closes silently" assertions), `src/lib/utils/sse-parser.ts` (composing event handler), `src/lib/hooks/useChat.ts` (composingMessage state), `src/components/MainApp.tsx` (prop threading), `src/components/mobile/MobileSession.tsx` (typing-indicator label + saved-to label reorder). 681 tests pass (was 678; +3 new fallback contract tests). Build clean.
- Checkpoint voice + UX fixes from 2026-05-14 testing (2026-05-14, happy-buck worktree): Four discrete fixes surfaced by testing the refactored pipeline against a real checkpoint ("Stomach Pushes Me to Fix the Call" — universalized from one example, body-as-agent title, dead-end after save, layer label too sparse). **(1) Headline rules reject body-as-agent.** The previous "good" examples in the composition prompt ("Voice Goes When Pressure Lands," "Body Locks Before Being Asked") were themselves poetic — body parts acting as agents. Replaced with first-person behavior-as-subject examples: "I Switch to Counter-Mode When Talked At," "I Tighten Before Answering," "I Run Interference on Call Tone," "I Freeze Before Someone Can Ask," "I Keep Teaching Him How to Live." "Stomach Pushes Me to Fix the Call" added explicitly to the bad examples. Explicit rule added: subject of the headline is "I" or the behavior itself, NOT a body part as agent. **(2) Composition stays within scope of evidence.** New VOICE RULE: "Stay within the scope of evidence the user gave you. If they described one example (one person, one situation, one moment), anchor the entry IN that example — name the person, name the context. Do not generalize to 'everyone,' 'every time,' 'all conversations.' Do not use 'always,' 'every,' 'all,' 'never' unless the user used those exact words themselves. One example produces a specific claim, not a universal one. Two or three convergent examples can support a wider claim — but only as wide as the examples actually span." Addresses the case where one Ryan call produced "I always get sucked into fixing how someone talks to me" — the universal claim wasn't supported by the evidence. **(3) Post-confirm Message 2 must end with a forward-moving question.** Both `first-message-2` and `subsequent-single` blocks now require: "The final line MUST be a question. It MUST end with a question mark." The previous "open-thread line" framing produced soft declarations ("Worth circling back to," "There's more to explore here") that read as dead-ends after a successful save. Good/bad examples added inline so Opus knows the target — bad: "Worth circling back to." Good: "Where else does this same impulse fire — only with Ryan, or other places too?" The post-confirm pause now propels forward instead of stopping the conversation. **(4) Chat-history label includes layer name.** `MobileSession.tsx` `Plate` for confirmed checkpoints used to render `"Saved to Layer Four"` only. Now appends the layer name: `"SAVED TO LAYER FOUR — HOW I SHOW UP WITH PEOPLE"`. `LAYER_NAMES` was already imported. Files: `src/lib/persona/confirm-checkpoint.ts`, `src/lib/persona/system-prompt.ts` (+ two pinned test assertions updated to match new "MUST be a question" wording), `src/components/mobile/MobileSession.tsx`. 678 tests pass. Build clean.
- Checkpoint pipeline refactor — deterministic detection + overlay hardening + post-confirm silent failures (2026-05-14, happy-buck worktree): Three related changes that simplify the checkpoint surface and close several silent-failure modes uncovered in user testing. **(1) Deterministic detection replaces the Haiku classifier.** The system prompt defined "I want to put something in your Manual" as the contract with the user, but a separate Haiku call sat on top of that contract judging whether the message was "really" a checkpoint — requiring a 4-8 word headline, the right structure, an inferable layer. When Haiku disagreed with the contract (e.g. the user-observed "Proof-Giving as Protection" case where the 3-word hyphenated headline tripped the gate), the card silently failed to surface even though Jove had committed to the user with a full proposal. New `src/lib/persona/detect-checkpoint.ts` is a regex on the transition line + minor variants ("I want to put this/that in your Manual"), case-insensitive, end-of-sentence anchored so stray references in unrelated prose don't match. `classifier.ts` and `classifier.test.ts` deleted. One fewer Haiku call per turn. **(2) Composition picks the layer.** Previously `composeManualEntry` took `layer` + `name` as inputs (from the classifier). Now it takes the full `manualComponents` (the user's Manual so far) and Opus picks both — based on the entry content and how it relates to entries already on each layer. Composition output adds a required `layer` field (1-5, validated); invalid layer → null return → checkpoint suppressed. Headline policy (the 4-8 words rule that tripped the old classifier) moved fully into the composition prompt where Opus is rewriting anyway. **(3) Gates simplified.** `applyCheckpointGates` no longer carries layer/name pass-throughs — returns `{ passed, reason }` only. Material-quality + turn-count gates still suppress before composition (cheap pre-emit check). `buildCheckpointMeta` takes `composedEntry` directly. **(4) CheckpointOverlay auto-close fix.** The 1.6s confirmed-cover dismiss timer was using `onClose` as an effect dependency. Because the parent (`MobileSession`) passed `onClose` as an inline arrow function, every parent re-render produced a new closure → effect re-ran → `clearTimeout + setTimeout`. During the post-confirm stream the parent re-renders constantly, so the timer kept resetting and the overlay sat stuck on the "Added to Layer N" cover. Fix: `onCloseRef` ref pattern, effect deps shrunk to `[confirmStatus, open]`. Escape also moved to the ref and now dismisses in every phase as a safety valve (was gated on `phase === "actions"` so a stuck confirmed cover had no escape hatch). **(5) Post-confirm scaffolding failures are silent.** Phase A (the manual_entries RPC write) is atomic + idempotent and always succeeds independently. Phase B (the post-confirm Sonnet call that produces the "scaffolding" follow-up message) was sharing the same SSE stream — when it failed, `emitError` fired "Jove lost the thread" and the client showed a "Connection lost" plate with a RETRY button wired to `retryLastMessage`, which would re-send a stale user message from earlier. Fix: `callPersona`'s top-level catch now branches on `postConfirmMode !== null` — closes the stream cleanly without emitting an error event. The entry stays saved, the prepended Message 1 ("In. A working name: 'X.' Yours to change.") stays in chat, conversation continues on the next turn. **(6) FirstCheckpointModal removed.** Dead code — not imported in production, replaced by inline trigger card + overlay. Removed: component, test, demo screen, two analytics functions (`trackModal3Shown`, `trackModalFlowCompleted`), comment references in `system-prompt.ts`. **Files.** New: `src/lib/persona/detect-checkpoint.ts` + test, `src/components/checkpoint/CheckpointOverlay.test.ts`. Modified: `src/lib/persona/call-persona.ts` (+ test), `src/lib/persona/confirm-checkpoint.ts`, `src/lib/persona/persona-pipeline.ts` (+ test), `src/lib/linq/persona-bridge.ts`, `src/components/checkpoint/CheckpointOverlay.tsx`, `src/lib/analytics/events.ts` (+ test), `src/lib/persona/system-prompt.ts` (+ test stale comment refs), `src/app/demo/glass/page.tsx`. Deleted: `src/lib/persona/classifier.ts` + test, `src/components/modals/FirstCheckpointModal.tsx` + test. Net 353 lines removed across the branch. 678 tests pass. Build clean.
- Admin nav consistency + desktop dev tools panel + "Jove's prompt architecture" rename (2026-05-14): Three related changes to the admin surface. **(1) Shared admin nav rail.** Extracted `src/components/admin/AdminNavRail.tsx` so `/admin`, `/admin/docs`, and `/admin/prompt-architecture` all render the same six-item nav (Users, Beta, Feedback, Health, Docs, Jove's prompt architecture). Fixes a bug where the Prompt Architecture link disappeared after clicking Docs — the docs page's hand-rolled nav was missing Health and Prompt Architecture entries entirely. Component accepts `activeId` for highlighting and an optional `badges` record so the main admin page can keep its unread-feedback and api-errors counts. **(2) Desktop dev tools panel.** New `src/components/admin/DevToolsPanel.tsx` extracts the Simulate user + Populate manual UI from `MobileSettings.tsx` into a standalone, compact component rendered in `DesktopVitrine.tsx`'s colophon column alongside the publication text. Admin-only (returns `null` for non-admins) and gated by a new `.mw-dev-tools` CSS class that only displays at ≥1030px (wide desktop), where there's room in the left margin. Lets admins simulate a user (1-3 checkpoints) and populate manual layers directly from the desktop canvas without opening mobile settings inside the phone frame. After populate completes, the page reloads so the phone frame's manual surface picks up the new entries. **(3) Rename.** "System Prompt" → "Jove's prompt architecture" across the page heading, the AdminNavRail label, and the desktop colophon link (`Prompts` → `Jove's prompts`). Files: `src/components/admin/AdminNavRail.tsx` (new), `src/components/admin/DevToolsPanel.tsx` (new), `src/app/admin/page.tsx`, `src/app/admin/docs/page.tsx`, `src/app/admin/prompt-architecture/page.tsx`, `src/components/layout/DesktopVitrine.tsx`, `src/app/globals.css`. 682 tests pass, build clean.
- Desktop colophon admin quick-nav (2026-05-13, epic-shannon worktree): The single "ADMIN" link in the desktop vitrine colophon expanded to a three-link row — `Admin · Prompts · Docs` — matching the editorial mono-caps dot-separator pattern used by the theme toggle and meta rows above and below it. Admin users now jump directly to `/admin`, `/admin/prompt-architecture`, and `/admin/docs` from the main canvas without going through the in-frame menu. Gated by `useIsAdmin()`; mobile users see nothing (the colophon itself is hidden by `.mw-desktop-paratext`). File: `src/components/layout/DesktopVitrine.tsx`. Build clean.
- Manual layer card redesign — burnished copper headers (2026-05-13, epic-shannon worktree): Each populated layer on the Manual page is now one unified card — the header and its entries share the same container with rounded corners, border, and backdrop blur. **Header treatment.** Burnished copper vertical gradient (`rgba(180,125,75,0.46)` → `rgba(135,88,52,0.34)`) with roman numeral, layer name in Spectral 18px/500, and entry count. **Entry rows.** Plain rows inside the card with `var(--session-walnut-surface)` background, separated by walnut-tinted dividers (`rgba(170,120,82,0.14)`). Expand/collapse with chevron rotation, "Explore further" button. **Empty layers.** Dimmed single rows with per-element opacity (roman numeral, name, count each at distinct alpha) instead of a blanket container opacity. **Intro modal removed.** The `mw_manual_intro_seen` localStorage-gated first-visit modal ("This is your manual…" + "Talk to Jove" / "Got it") deleted — the Manual page now loads clean without interception. **Page header.** Bottom padding tightened from 24px to 20px. **Files.** `PopulatedLayer.tsx` (rewritten), `EntryItem.tsx` (rewritten — `showDivider` prop, `soWhat` section removed per upstream merge), `EmptyLayer.tsx` (rewritten), `MobileManual.tsx` (modal removed, padding fix). HTML mockups committed at `mockups/manual-populated-{1,2,3}.html`. 682 tests pass. Build clean.
- New vs returning user prompt restructure (2026-05-13, suspicious-mirzakhani worktree): Structural fix to how `buildTier3()` renders user-status blocks across conversation modes. **Problem.** FIRST MESSAGE (new user) and RETURNING USER blocks both rendered alongside guided-intake/upload mode blocks, giving the model conflicting opener instructions. A returning user clicking "Navigate a situation" got "I'm Jove. Tell me what's going on." because the old RETURNING USER three-part template ("Welcome back / Manual ref / What is on your mind today?") assumed Jove speaks first, but the app always has the user speak first via landing cards. **Fix.** (1) `showFirstMessage` gated on `mode === "situation"` — FIRST MESSAGE no longer renders alongside guided-intake/upload openers that have their own instructions. (2) Guided-intake and upload OPENER sections now branch on `isReturningUser`: returning users get "deliver the opener without introducing yourself or greeting them"; new users get "you may briefly introduce yourself before the opener." (3) RETURNING USER block split: general rules (no intro, no recap, follow their lead — all modes) + situation-specific first-turn block (reference Manual, respond to what they said, activated carve-out — situation mode only). (4) FIRST SESSION text cleaned up — removed stale "welcome chip" reference. **Files.** `src/lib/persona/system-prompt.ts`, `src/lib/persona/system-prompt.test.ts` (4 new tests for mode×user-type matrix, 3 tests updated). 684 tests pass. Build clean.
- Remove so_what as separate field (2026-05-13, happy-buck worktree): The "what changes now" content was a separate `so_what` column / field / UI section. Merged into the entry's continuous prose — the composition prompt now includes stance naturally in the passage instead of producing a separate JSON field. Removed from: composition prompt output, RPC params (8→7), `CheckpointMeta`, SSE `message_complete` events, `ActiveCheckpoint` type, `ManualEntry` type, `CheckpointOverlay` rendering, `EntryItem` rendering, `manual-context.ts` full-text and mixed-voice logic, `layer-definitions.ts` adapter, `sse-parser.ts` checkpoint type. Migration `20260513200000_remove_so_what.sql` drops the column (never applied to prod) and recreates the 7-param RPC. System prompt and extraction brief hints updated to say "what changes" instead of "the so-what." 653 tests pass. Build clean.
- Checkpoint voice + card + backdrop fixes (2026-05-13, happy-buck worktree): Five fixes from user testing focused on "descriptive, not performative." **(1) Lead-in text.** Changed from "I'm seeing something here" / "Something else is taking shape" to "A pattern came through in what you said." for both first and subsequent checkpoints. **(2) Composition prompt anti-nominalization.** Added explicit rule: "No nominalizations ('the reach,' 'the ask,' 'the pull'). Use verbs, not nouns pretending to be verbs" with before/after example ("I turn away before the reach" → "I turn away before they can ask me"). Added "Write like a field note, not literature" voice rule. Headline bad examples expanded: "Turned Away Before the Ask" flagged as nominalization; good example corrected to "Body Locks Before Being Asked." **(3) Trigger card layer header.** Redesigned from flat card with "SUGGESTED MANUAL ENTRY" eyebrow to two-section card: top section with `--session-walnut-highlight` background showing layer name ("Layer One — Wiring"), bottom section with entry title + "Tap to review" + chevron. `LAYER_NAMES` and `LAYER_ORDINAL` maps used for display. **(4) Modal backdrop opacity.** `--session-backdrop-heavy` increased — dark mode 0.75→0.92, light mode 0.55→0.85 — so conversation text behind the checkpoint overlay no longer bleeds through. 655 tests pass. Build clean.
- VC-readable prompt architecture page (2026-05-13, romantic-colden worktree): Admin-gated page at `/admin/prompt-architecture` that renders the Jove system prompt as an annotated scrolling document, importing directly from the live composition functions — no copy-pasted prompt text. **Route.** `/admin/prompt-architecture` page + `/api/admin/prompt-architecture` API route. Page uses `useIsAdmin()` hook (matching existing admin pattern). API route uses `verifyAdmin()` + middleware gating. **Layout.** Sticky header with title, persona checkboxes (multi-select, General mutually exclusive with neurotypes), conv-mode radio (Standard / Guided Intake / Upload), and phase nav anchors. Four lifecycle phases rendered below: (1) Brand new account, (2) Approaching first checkpoint, (3) Returning user, (4) Returning user approaching checkpoint. Each phase calls `buildSystemPrompt()` with appropriate flags. **Per-section annotations.** Each prompt section shows: tier-coded left border (blue intro / brown T1 / amber T2 / green T3 / purple dynamic), first 3 lines of actual prompt text with fade-out + "Show full text" toggle, token count (~chars/4), "why" condition pill (Always / Persona / State / Conv mode / Dynamic), slot-in alternatives with token counts, and info button opening a modal with full text + source file reference. **Phase footers.** Total tokens, section count, delta vs previous phase, bulleted "what changed" list (added/removed sections). **Section parser.** `src/lib/admin/prompt-sections.ts` parses rendered `buildSystemPrompt()` output into labeled sections using stable header patterns, maps each to source modules, computes token estimates, and generates alternatives. **Replaces** `docs/prompt-architecture.html` (deleted — the three-view prototype that taught us the design). **Files.** `src/lib/admin/prompt-sections.ts` (parser + phase builder), `src/app/api/admin/prompt-architecture/route.ts` (API), `src/app/admin/prompt-architecture/page.tsx` (page). **Nav.** Linked from admin sidebar rail (below Docs, above ← EXIT ADMIN). **Visual redesign v2** (2026-05-13): Two-column slot-in layout. Page now renders full admin nav rail (Users, Beta, Feedback, Health, Docs, Prompt Architecture) matching the main admin page. Four phases collapsed by default as accordions showing summary stats (tokens, sections, delta). Each phase expands to a two-column view: left column "Available Components" lists absent sections (not active in this phase) with tier indicators, token counts, condition pills, and "Active in: ..." labels, plus "Swap Alternatives" for persona/mode-dependent sections; right column "Active Prompt" shows the rendered prompt as clearly separated section cards with tier dots, condition pills, token counts, "Show full text" toggle, and info modal. Persona/mode controls at top drive which components appear in each column. Build clean. **Visual redesign v3 — three-layer cake** (2026-05-13): Replaced the four-phase accordion with a three-layer conceptual model that groups sections by what changes them rather than when they change. Layer 1 "Foundation" (collapsed by default): 14 always-present sections (constitutional rules, banned phrases, pacing, repair — persona/state-independent). Layer 2 "Voice" (open by default): 5 persona-dependent sections (voice intro, voice rules, example register, landing, deepening) with "OTHER VOICES" alternatives bar showing unselected personas' token counts. Layer 3 "Lifecycle" (open by default): 9 conditional blocks (first-message, returning-user, checkpoints, readiness gate, confirmed manual, session context, etc.) each with "Active in:" phase indicator pills showing which of the 4 lifecycle phases include it. Controls: persona multi-select chips + conv-mode chips + total token count. Each section card: tier dot, mono-caps label, token count, info button (modal with source file reference + full text), truncated preview with "Show full text" toggle. Reduces cognitive load from 4×20+ sections to 3 clear categories (~70% foundation, ~15% voice, ~15% lifecycle). Build clean.
- Multi-select persona modes + equal voice stacking (2026-05-13, mystifying-dewdney worktree): Persona mode expanded from single-select to multi-select, and Tier 2 voice composition refactored to stack personas equally rather than the previous primary-plus-additions optimization. **Schema.** `profiles.persona_mode` (text, single value) → `profiles.persona_modes` (text[], multi). Migrations applied in three steps: `20260513100000_expand_persona_mode.sql` widens the CHECK constraint to include audhd + dyslexic + general, `20260513110000_add_audhd_dyslexic_persona_modes.sql` is an intermediate step, `20260513120000_persona_modes_array.sql` converts the column to text[] with `{autistic}` default, drops the old column, and adds a subset CHECK against the four valid values. **UI.** `PersonaModeScreen` converted from radio to checkboxes. State is `PersonaMode[]` rather than `PersonaMode`. Checking "general" unchecks neurotype-specific modes and vice versa; continue is disabled when nothing is selected. DB write goes to `persona_modes` array. The screen renders inside `PostLoginOnboarding` (the post-auth gate), not `OnboardingFlow` (which is now just entry + login). **Equal voice stacking.** Previous design: the first selected persona contributed its full ~1,400-token Voice block; every additional persona contributed only a ~60-token `UNIQUE_ADDITIONS` summary block. Token-efficient but conceptually wrong — it introduced a "primary" hierarchy and lost ~90% of each additional persona's unique behavioral guidance (LANDING_EXAMPLES, WEAK_STRONG_EXAMPLES, voice-rule mutations, deepening additions). New design: new `src/lib/persona/voice-scaffold.ts` holds the ~900 tokens of structural content duplicated across all four voice files (TIER 2 header, BANNED_PHRASES + BANNED_PATTERNS, dash-to-period rule, LANDING intro, DEEPENING intro/outro, PACING rule, WHEN_JOVE_IS_WRONG, WHEN_USER_ASKS_WHAT_SHOULD_I_DO). Each `voice-*.ts` shrinks to ~500 tokens of persona-unique data: `VOICE_INTRO_PARAGRAPHS`, `VOICE_RULES`, `EXAMPLE_REGISTER`, `LANDING_EXAMPLES`, `DEEPENING_ADDITIONS`, `WEAK_STRONG_EXAMPLES`. `renderTier2()` removed from each module. `composeTier2(modes)` in `system-prompt.ts` rewritten to assemble scaffold + every selected module's unique content, deduplicating by full content (label+line for example_register and landing_examples; weak+strong for weak_strong; full string for voice_rules and intro paragraphs) so persona-specific variants with the same label both appear (e.g. autistic's "Naming a pattern" AND audhd's "Naming a pattern" both render in multi-mode). `UNIQUE_ADDITIONS` and `TIER2_RENDERERS` deleted. General is filtered out when combined with neurotype-specific modes (existing behavior preserved). **Behavior guarantees.** Single-mode prompts are byte-identical to pre-refactor (verified by snapshot diff against `renderTier2()` output before deletion). Multi-mode prompts grow ~440 tokens per additional persona vs the old ~60. Worst case three-persona prompt lands around ~6,300 tokens — well within budget. Within-section ordering follows the user's selection order (autistic-first vs dyslexic-first puts that mode's examples first in lists), but content weight is symmetric. **Prompt architecture visualizer.** Originally shipped as `docs/prompt-architecture.html` (single-file prototype with three views). Replaced by the admin-gated `/admin/prompt-architecture` page that imports live composition functions — see separate entry above. **Tests.** Tests refactored for equal stacking: BANNED_PHRASES/BANNED_PATTERNS imports moved from `voice-autistic` to `voice-scaffold` (single source of truth); the `composeTier2 multi-select` describe block rewritten to assert both intros present, both unique behavioral guidance present, shared scaffold (WHEN JOVE IS WRONG, etc.) appears exactly once. 675 tests pass (up from 650). Build clean.
- Checkpoint trigger card + overlay review (2026-05-13, amazing-hertz worktree): Reworked the pending-checkpoint moment from an inline content card with text-link actions into a two-stage reveal — building indicator → compact trigger card → full-screen overlay review. Trigger card shows the eyebrow, the composed entry title, and "Tap to review"; the entry body lives in the overlay, not inline in the chat stream. **New component** `src/components/checkpoint/CheckpointOverlay.tsx` renders the unified module (entry + actions in one card). Entry section shows `composedContent` (the polished version that will actually land in the Manual) with fallback to the assistant message content for older builds. Actions row is "Add to my Manual" primary, "Edit" toggle (contenteditable on headline + body with a hint line), "Jove, let's rework together" (refine), and "Discard". Refinement ceiling preserved as the existing "Put it in as it is / let it go" fork inside the same overlay. **Lead-in now fires for every checkpoint.** `call-persona.ts` previously emitted the transition line ("Something else is forming…") only when `manualComponents.length >= 1`. First-time checkpoints landed cold. The condition was relaxed to always-emit; first-checkpoint variant uses a softer "Hold on — I want to put something in front of you." subsequent variant unchanged. The lead-in is a persisted assistant message inserted 1s before the checkpoint row, so it survives reload and flows into future Jove context. **Edited content reaches the Manual.** `/api/checkpoint/confirm` accepts optional `editedContent` and `editedName`; `confirmCheckpoint()` overrides `composed_content`/`composed_name` when present, recomputes the summary fallback from the edited text, and clears stale composed key_words so the compressed view reflects user-authored words. `useChat.confirmCheckpoint()` plumbs an `edits` object through to the API and to the optimistic local entry insert; `setMessages` also rewrites the in-memory checkpoint message so the chat history's historical Plate shows the final committed text rather than the unedited proposal. **Composed content streams to the client.** `message_complete` SSE event now carries `composed_content` and `composed_name` alongside the checkpoint payload. `ActiveCheckpoint` and the DB-load path in `useChat` both surface them. **Overlay confirm waits on the API.** Replaced the fixed-timer composing → confirmed sequence with a `confirmStatus` prop (`idle`/`pending`/`success`/`error`) driven from parent state — composing shows while the call is in flight, confirmed only on success, and an error line renders above the actions so the user can retry from the same surface. **StrictMode fix.** The building-indicator timer initially gated on `!prevCheckpointRef.current`, which in Next.js dev StrictMode (effects run setup→cleanup→setup) saw the ref already populated on the second setup and skipped scheduling — trigger card never appeared. Switched to keying off `activeCheckpoint?.messageId`; both prod and dev now behave correctly. **FirstCheckpointModal copy** updated from "A pattern is ready for your Manual" / "I have a pattern to put in front of you…" to "A suggested entry is ready" / "I have something to put in front of you. Tap to review it. If it fits, add it to your Manual. If it's off, we can rework it together." **Tests.** `onboarding-copy.test.ts` updated for the new action labels living on `CheckpointOverlay.tsx`; `FirstCheckpointModal.test.ts` updated for the new copy. 650 tests pass. Build clean.
- Checkpoint bug fixes (2026-05-13, happy-buck worktree): Four fixes from user testing. **(1) Greeting for returning users.** Jove was introducing itself by name ("Hello I'm Jove") on returning-user sessions. FIRST MESSAGE block now allows self-introduction on the very first session; RETURNING USER block explicitly prohibits it ("Do not introduce yourself by name — the user already knows who you are"). **(2) Performative titles.** Composition prompt and checkpoint delivery instructions strengthened against poetic/literary titles (e.g. "Gaps Open and the Reach Fires"). Added explicit anti-poetry instruction and bad examples. **(3) Error text contrast.** Checkpoint overlay error message was brown-on-brown (muted salmon on walnut surface). Changed to `--session-ink` for readability. **(4) confirm_checkpoint_write RPC migration.** The `p_so_what` parameter was missing from the production function (old 7-param signature persisted). Re-applied the function portion of `20260513100000_add_so_what_column.sql` — `pronargs` now 8. Confirm + edit flow works. Tests added for so_what, editedContent, and editedName paths. 655 tests pass. Build clean.
- Checkpoint polish pass (2026-05-13, happy-buck worktree): Four fixes from manual testing. **(1) Lead-in voice.** Changed from performative ("Hold on — I want to put something in front of you") to descriptive ("I'm seeing something here" for first checkpoint, "Something else is taking shape" for subsequent). **(2) FirstCheckpointModal removed.** The modal that intercepted the first-ever checkpoint is gone — inline trigger card + overlay flow runs every time. `modal_progress` auto-advances to 3 via fire-and-forget POST. **(3) Discarded checkpoints collapse.** Rejected checkpoints now show only the entry title + "Discarded" status label — no full content. Confirmed and refined checkpoints still show the full text. **(4) Post-discard response.** Changed from "Okay. What made it miss?" to "That entry didn't land. Was it off, or just not ready?" — clearer about what happened. **(5) So-what label removed.** The "WHAT CHANGES" mono-caps eyebrow was over-labeling; replaced with hairline separator + italic styling only in both CheckpointOverlay and EntryItem. Tests updated. 650 tests pass. Build clean.
- Checkpoint redesign — three-phase flow, so-what entries, Opus composition (2026-05-13, happy-buck worktree): Redesigned the checkpoint and manual entry system across extraction, classifier, composition, system prompt, and UI. **New entry structure.** Manual entries now have two parts: the pattern (statement + passage) and an optional so-what (the user's stance — what changes now that they can see the pattern). New `so_what text` column on `manual_entries`; `confirm_checkpoint_write` RPC updated to accept `p_so_what`. **Three-phase conversation flow.** Extraction now tracks `pattern_engaged` (boolean), `user_named_cost`, and `user_named_stance` in extraction state. Jove receives phase-aware hints: pattern not yet named → keep exploring; pattern engaged → work toward the so-what; material ready → checkpoint can fire. Turn-12 override bypasses `pattern_engaged` if extraction missed the engagement signal but material quality is otherwise strong. **Classifier hardened.** Checkpoint detection now requires all of: the pinned transition line ("I want to put something in your Manual"), sustained reflection, headline, and validation question. Reflections without the transition line are classified as naming moves, not checkpoints. **Composition on Opus.** Switched from Sonnet to Opus (`claude-opus-4-6`) with `max_tokens: 2048`. Composition prompt rewritten for first-person voice, two-part structure (pattern + so_what), new exemplars. The so-what returns null when the user hasn't landed on a stance — no canned text fabricated. **System prompt updates.** Added "NAMING THE PATTERN" section with three naming shapes (pointing at repetition, offering plain description, naming contradiction). Two-instance rule and strength exception. Checkpoint delivery updated with so-what step. **Manual context compression.** Recent entries render `so_what` as "What changes: ..." below the pattern. Mixed-voice note appended when both old (you-voice, no so_what) and new (I-voice, has so_what) entries coexist. **UI.** `CheckpointOverlay` renders `composedSoWhat` below the entry body in italic with a hairline separator. `EntryItem` renders `soWhat` on manual entry cards the same way. `ActiveCheckpoint` type, SSE event, and `useChat` all carry `composed_so_what` / `composedSoWhat`. **Text channel.** `persona-bridge.ts` passes `turnCount` to `applyCheckpointGates`. **Tests.** Updated extraction state defaults and checkpoint-gate tests for `pattern_engaged`. 650 tests pass. Build clean. **Migration required.** `supabase/migrations/20260513100000_add_so_what_column.sql` must be applied before deploy — adds `so_what` column and recreates the RPC.
- Bubble color flip (2026-05-13): Reversed the color assignment across the chat surface. Jove bubbles now use the cooler slate/navy surface (`--session-bubble-user`), user bubbles use the warm walnut surface (`--session-walnut-surface`). Background gradient flipped in both dark and light modes — walnut warmth pools at the top of the viewport, navy/slate deepens toward the bottom. Input pill (`ChatInput.tsx`) matches the bottom navy zone with `--session-bubble-user` bg + border. Checkpoint plates unchanged (still walnut-surface — they hold user-confirmed Manual content). Files: `globals.css` (dark + light `--session-bg-chat` gradients), `Bubble.tsx` (swapped `JOVE_STYLE`/`USER_STYLE` backgrounds and borders), `ChatInput.tsx` (pill surface + border tokens). Build clean, 650 tests pass.
- UI polish pass (2026-05-13): Six fixes across drawer, entry screen, session lifecycle, admin, and API error handling. **(1) New-session lag fix.** `startNewSession()` in `useChat.ts` blocked on two awaits — `fetch("/api/conversations/complete")` and `refreshConversations()`. Both changed to fire-and-forget (`.catch(() => {})` without `await`), matching the `startExploration()` pattern. Button response is now instant. **(2) Drawer polish.** "Read my Manual" renamed to "my Manual" and moved first in the pill pair; "+ New session" second. Session list collapsed to 3 visible rows with "Show all (N)" expander — resets on drawer close. Single-line session rows (title + date, removed message counts). Added "Log out" NavRow between Settings and Crisis support; `onLogout` prop threaded through `MainApp` → `SessionDrawer`. **(3) Entry screen single CTA.** Closed-beta landing simplified from dual-CTA ("Begin" + "Already have access? Sign in") to a single "Sign in" pill. `onBegin` made optional, primary button fires `onLogin`. Tests updated. **(4) Admin colophon link.** Desktop vitrine footer shows "ADMIN" link (mono caps, `/admin`) for admin users only. Uses `useIsAdmin()` hook (`user?.app_metadata?.role === "admin"`). **(5) Session summary false-500 fix.** `/api/session/summary` route returned 500 + recorded an API error when `generateSessionSummary` returned null (expected for new conversations or transient Anthropic failures). Changed to return 200 with `{ summary: null }` — the client handles missing summaries gracefully. **(6) displayName bug fix.** `useChat.ts` now exposes `displayName` state alongside `firstName`, fixing a `ReferenceError` when `displayName` was referenced but never declared. 650 tests pass, build clean.
- First name only when addressing the user (2026-05-12, fervent-austin worktree): New `firstNameFrom` utility at `src/lib/utils/name.ts` returns the first whitespace-separated token of a display name, with `"User"` fallback for null/empty. Threaded through the data boundary in `useChat.ts` — the hook now exposes `firstName` instead of `displayName`. Single current consumer is `MobileManual.tsx` (PDF export header/title), so the user's PDF title reads "Jeff's Manual" instead of "Jeff Waters' Manual" — but the plumbing is the point: any future surface that addresses the user (greetings, salutations, addressed copy) reads `firstName` and gets the right register by default. Linq's `group-bridge.ts` and `group-detection.ts` keep their nullable local extraction (`displayName?.split(/\s+/)[0] ?? null`) — they treat `null` as "unnamed" rather than defaulting to `"User"`, and changing that semantics would affect message routing. 10 new unit tests cover the utility (single token, multi-word, hyphenated, diacritics, null/empty fallback, whitespace collapse). 636 tests pass, build clean.
- Navigation audit + fixes (2026-05-12, fervent-austin worktree / `claude/light-mode-implementation`): Comprehensive audit found 8 navigation problems across the drawer + TopBar + sub-views. All fixed in one branch. **(1) Glyph contrast in light mode.** `CIRCLE_BTN_STYLE` in `TopBar.tsx` and the drawer close button in `SessionDrawer.tsx` hardcoded `color: rgba(255,255,255,0.85)` — near-invisible on the new linen ground. Same hardcode in `ChatInput.tsx`'s mic/stop SVG stroke. All three now use `var(--session-ink)` (espresso in light, cream in dark). The inner "glass" border switched from `rgba(255,255,255,0.10)` to `var(--session-walnut-border-soft)` for proper definition in both modes. **(2) HIG tap targets.** TopBar circle buttons 30→44, drawer close 28→44. Glyph font-size adjusted (13.5→16 for TopBar, 12→14 for drawer close) so the visible mark stays at the editorial register without ballooning. TopBar vertical padding reduced 14→10 to keep header height balanced with the larger circles. **(3) Back affordance for sub-views.** `TopBar` API extended to accept both `onBack` and `onMenu` simultaneously. When both pass, back chevron occupies the LEFT slot (iOS convention) and menu moves to the RIGHT. When only one passes, behavior unchanged — onboarding screens (LoginScreen/SeedScreen/InfoScreens) keep their solo back chevron; chat keeps its solo menu. `MobileSettings` and `MobileManual` now pass `onBack={onNavigateToSession}` alongside `onMenu`. **(4) Feedback pill hidden on sub-views.** With the menu glyph now in the TopBar's right slot for sub-views, the floating `BetaFeedbackButton` (absolute top:22 right:22) collided. `MobileLayout` renders it only when `activeView === "session"` — feedback stays prominent on chat, hidden on Settings/Manual/Crisis. **(5) "+ New session" actually returns to chat.** `MainApp` wraps `startNewSession` in `handleNewSession` that flips `activeView` to `"session"` before resetting. Previously: tapping from Settings or Manual reset state but stranded the user on the wrong panel — the button looked broken. **(6) Drawer active state.** `NavRow` accepts `isActive`; renders a 2px walnut left rail (matching the active session row pattern) and lifts text from `--session-ink-soft` to `--session-ink` when current. Crisis row uses the oxblood rail. `MainApp` passes `activeView` through to the drawer. **(7) Drawer a11y.** `useEffect` autofocuses the close button on open and listens for Escape on document; either closes. The `aria-modal="true"` claim is now backed by keyboard behavior. **(8) Crisis Support as its own surface.** New `MobileCrisis.tsx` view with the editorial pattern — oxblood eyebrow + Spectral heading ("If you need someone now.") + 2-sentence intro + two link cards (988 + 741741, both `href="tel:"` / `href="sms:"`) + reassurance footer ("Free · Confidential · Available 24/7"). `MobileLayout` extends the `MobileView` union to include `"crisis"` and renders the panel alongside session/manual/settings. The duplicated Crisis section in `MobileSettings.tsx` was removed (Settings stays focused on settings; Crisis gets a real destination). Drawer's Crisis row routes to `onNavigateToCrisis()` instead of `onNavigateToSettings()`. **(9) In-frame wordmark visible at desktop wide.** Removed the `.mw-phone-frame .mw-topbar-wordmark { visibility: hidden }` rule at ≥1030px. The previous assumption (outer 52px masthead replaces in-frame wordmark) left the phone frame feeling unbranded. Both wordmarks now render at different scales: outer at publication 52px on the canvas margin, inner at UI 19px in the TopBar — different roles, not duplicates. **Result.** 1 merge commit, 9 files touched (1 new), 626 tests pass, build clean. Verified in browser at 410×840 and 1440×900 in both light and dark modes: menu glyph readable, back chevron lands you on chat, drawer shows active rail for current sub-view, Crisis surface renders editorially, "+ New session" works from any view. **Post-ship polish.** Three follow-up items: (a) Drawer hierarchy reordered — "Read my Manual" moved from the bottom NavRow to a top-of-drawer pill with equal visual weight to "+ New session". New shared `PrimaryPill` component wraps both: walnut surface + walnut border, optional entry-count badge (Manual shows "N entries"), active-state walnut left rail + card shadow. The two most-tapped entry points (start fresh / open Manual) now sit together at the top; Settings stays in the secondary nav. (b) Swipe-from-left-edge to open the drawer. Document-level passive touch listeners in `MainApp` detect `touchstart` within 24px of the left edge, then a horizontal swipe ≥60px that's clearly horizontal (`dx > 1.5 * dy`). Disarms if vertical scroll wins (`dy > 30` before threshold). Only active when drawer is closed. Verified by dispatching synthetic `TouchEvent` from edge — drawer transitions from `aria-hidden=true` to `false` after the swipe. (c) Exploration chip dismiss `✕` bumped from 22×22 → 44×44 with negative right-margin so layout doesn't shift; color upgraded `--session-ink-mid` → `--session-ink`; glyph 12→14. Closes the last sub-44pt tap target in the nav surface.
- Welcome entry cards + rotating greeting (2026-05-12, angry-shockley worktree): Replaced the three text-based `WELCOME_CHIPS` and scattered "Help me get started" links with a unified entry-point selector. **Welcome header.** Date in uppercase mono-caps (`formatWelcomeDate()`) + 44px serif greeting with accent-colored walnut period + italic lead-in "What's on your mind today?". First-time users see "Hello,\nI'm Jove." (two-line via `white-space: pre-line`). Returning users get a random greeting from `RETURNING_GREETINGS` (6 options, some personalized with `displayName` when `hasRealName` is true — skips email-prefix handles by checking for at least one capital letter and not "User"). Greeting index randomized per session via `useMemo` with `conversationId` dependency. **Three entry cards.** (1) "Navigate a situation" — sends `"I have a situation I want to work through"` to Jove, kickstarting the conversation (restored in 2026-05-13 onboarding cleanup; previously just dismissed cards without sending). (2) "Guided intake" — calls `startGuidedIntake()`, Jove speaks first with the relationship-anchored opener. (3) "Upload" — calls `startUpload()`, Jove speaks first with the paste-content opener. Each card is a flex row: 36px rounded icon square (inline SVG) + serif title + sans subtitle + chevron. Cards use `--session-walnut-surface` bg, `--session-walnut-border` border, 14px radius (updated 2026-05-13 to match walnut glass design). Date eyebrow uses `--session-walnut-meta` (warm tone). 36px gap between subtitle and cards. `chipsVisible` state resets on `conversationId` change. All three empty-state render paths (first-time, returning, fallback) collapsed into one `showEntryCards` block. **Onboarding cleanup (2026-05-13).** Removed 5 ghost props from `MobileSessionProps` (`isNewUser`, `firstSessionCompleted`, `sessionOrigin`, `sessionSummary`, `lastSessionDate`) — declared in interface and passed from `MainApp` but never destructured or read in the component body. Corresponding destructuring and JSX prop-pass removed from `MainApp`. `OnboardingFlow.tsx` cleaned of dead code: unreachable `"onboarding"` and `"seed"` views removed (no handler ever transitioned to them), `handleNavigateToSeed` deleted, unused `InfoScreens`/`SeedScreen` imports removed, `ViewName` simplified to `"entry" | "login"`. `PostLoginOnboarding` (the post-auth gate at `/`) still renders `InfoScreens` → `SeedScreen` — that path is live and unchanged. **Tests.** `onboarding-copy.test.ts` updated: removed negative assertion for kickstart message string, added positive assertion for `sendMessage("I have a situation I want to work through")`. 651 tests pass. Build clean.
- Relationship-anchored guided intake opener + quick-reply chips (2026-05-12, angry-shockley worktree): Two changes shipping together as the guided intake cold-start entry point. **(1) Relationship-anchored opener.** `GUIDED_INTAKE_OPENER` changed from the moment-based opener to "Pick someone of note in your life. A person — or a pet. Could be your partner, parent, a friend, a coworker. Positive or negative, high impact or low. Just someone worth naming." GUIDED INTAKE block in `system-prompt.ts` rewritten: OPENER section (locked text), FALLBACK CHAIN (widen scope → relationship to pattern → gentle end), AFTER NAMING (acknowledge → orienting question → scene invitation), QUICK-REPLY OPTIONS (format spec + behavioral rules), POSTURE/DIRECTED MOVES/PIVOTS/EXIT (carried forward). Detection anchors updated: `RECENCY_DROP_PHRASE` → `WIDEN_SCOPE_PHRASE` ("Who did you last have a conversation with"), `MOMENTS_TO_STATES_PHRASE` → `RELATIONSHIP_TO_PATTERN_PHRASE` ("Skip the person"). `GuidedIntakeOpenerVariant` type updated to `"widen_scope" | "relationship_to_pattern"`. **(2) Quick-reply chips.** Backend: `call-persona.ts` parses `---chips---` delimiter, strips chip lines from `conversationalText`, emits `chips` array on `message_complete` SSE event (capped at 6). Chips are ephemeral — stripped before DB storage, not persisted on assistant messages. `sse-parser.ts` + `types.ts` extended with `chips?: string[]`. Client: `useChat.ts` stores chips on the message, exposes `sendChipResponse(text)` which clears all chips and sends with `isChipResponse: true` flag. `QuickReplyChips.tsx` component renders chips as a vertical stack of serif italic buttons with hairline borders. `MobileSession.tsx` renders chips below the last assistant bubble (hidden during streaming). API: `route.ts` accepts `isChipResponse`, `call-persona.ts` stores `{ chip_response: true }` in message `metadata`, `persona-pipeline.ts` annotates chip-tap messages with `[selected from options]` in history so Jove follows up for texture. **Migration.** `20260513000000_add_message_metadata.sql` adds `metadata jsonb DEFAULT '{}'` to `messages` table. **Tests.** Chip parsing, SSE protocol, variant detection, UI wiring assertions. 640 tests pass. Build clean.
- Upload entry point — backend + pipeline (2026-05-12, romantic-banzai worktree): Third conversation mode (`"upload"`) for users to paste text content (chat logs, email threads, journal entries) for Jove to analyze against their Manual. **URL detection removed.** Inline URL detection (`detectUrls`) and content fetching (`fetchUrlContent`) stripped from the persona pipeline (`call-persona.ts`). The `SHARED CONTENT` and `SHARED CONTENT — FETCH FAILED` dynamic prompt blocks removed from `system-prompt.ts`; `contentContext` param removed from `BuildPromptOptions`. `url-detection.ts` and `fetch-url-content.ts` files retained in the codebase for the future Resonant Content entry point. **Transcript detection kept as passive fallback.** `detectTranscript()` still runs on every non-upload message at zero cost (pure regex). Upload-mode conversations skip it — the Tier 3 block handles content analysis directly. **Upload Tier 3 prompt block.** New conditional block in `buildTier3()` when `mode === "upload"`. Deterministic opener via `UPLOAD_OPENER` constant in `src/lib/persona/upload-copy.ts` ("Paste something here. A text thread, an email chain…"). After the user pastes, Jove identifies format (speaker-alternating, email thread, journal, unknown), acknowledges with a specific reference, asks a framing question, then cross-references against Manual entries. Behavioral guardrails: focus on the user's behavior, no diagnosis of others, no summarizing, no advice. **Plumbing.** Migration `20260512000000_add_upload_mode.sql` adds `'upload'` to `conversations.mode` CHECK constraint. `EntryPoint` and `ConversationMode` analytics types expanded. Chat route accepts `"upload"` mode; message length limit raised to 16,000 chars for upload-mode conversations (vs 4,000 standard). When continuing an existing upload conversation, route reads mode from DB so the higher limit applies to follow-up messages (the paste arrives as the second message, after Jove's opener). `startUpload()` in `useChat.ts` follows the guided-intake pattern: `message: null`, Jove speaks first. `FABRICATED CONTENT` guard simplified — Jove can no longer access URLs, just tells users to paste or describe. **Tests.** 629 pass (from 628 — URL tests replaced with upload mode + fabrication guard tests). Build ✓. **UI partially wired** — Upload card appears on the welcome entry screen (disabled placeholder at 40% opacity). Backend + pipeline ready; card needs to call `startUpload()` once enabled.
- Light-mode redesign (2026-05-12, fervent-austin worktree / `claude/light-mode-implementation` branch, all 8 phases per `docs/implementation/light-mode.md`): Light mode added alongside the already-shipped dark mode. Source of truth: `src/app/demo/linen/page.tsx`, **Version 03 — Embossed linen**. Walnut spine identical across themes (`#5C3A1E`) — only the surrounding tone shifts. **Foundation (Phase 1).** `useTheme` hook (`src/lib/hooks/useTheme.ts`) returns `{ theme: 'system'|'light'|'dark', resolved, setTheme }`, persists to `localStorage.mywalnut.theme`, subscribes to `matchMedia('(prefers-color-scheme: light)')` for live system-pref changes. `ThemeInit` (`src/components/ThemeInit.tsx`) is a mount-once client component that keeps `<html data-theme>` and `<meta name="theme-color">` in sync. `layout.tsx` adds a FOUC-safe inline script in `<head>` that synchronously reads localStorage → falls back to `matchMedia` → falls back to dark, then sets `data-theme` and the PWA `theme-color` before paint. SSR default remains `data-theme="dark"` with `suppressHydrationWarning`. **Tokens (Phase 2).** `[data-theme="light"]` block in `globals.css` with the full V3 Embossed token set — linen ground `#E5D8BE`, espresso ink `#1F140A`, walnut spine `#5C3A1E`, ink hierarchy (`--session-ink`, `--session-ink-soft #4A3220`, `--session-ink-faded #7A5E40`, `--session-ink-mono #5C3F23`), surface tints (`--session-walnut-faint 0.18`, `--session-walnut-tint 0.08`), bubble/plate paper tones (`--session-bubble-jove rgba(232,220,196,0.90)`, `--session-bubble-user rgba(92,58,30,0.16)`, `--session-plate rgba(236,224,200,0.93)`), shadow stacks (`--session-bubble-shadow`, `--session-plate-shadow`, `--session-card-shadow`), sage `#5E7A4F`, oxblood `#7A2E2E` (cross-theme, unchanged). Plus a `@media (prefers-color-scheme: light) { :root:not([data-theme="dark"]) { color-scheme: light; } }` safety net for no-JS fallback. **Per-surface gradients (Phase 3).** Four light gradient stacks in `globals.css` — `--session-bg-welcome`, `--session-bg-chat`, `--session-bg-checkpoint`, `--session-bg-manual` — byte-identical to the demo's `C` object: cream highlight at top, caramel pool mid, deep walnut at corner. Verified via `getComputedStyle().backgroundImage` against the demo. `DesktopVitrine.tsx` corner vignette switched from a hardcoded `rgba(26,22,20,0.02)` to `var(--session-vignette)` so it flips with theme. **Component audit (Phase 4, the highest-risk sweep).** 15 components scanned for hardcoded RGBs/hexes that would break in light. 18 new tokens added at end of `:root` and matching values in `[data-theme="light"]` to cover what surfaced: `--session-bubble-border`, `--session-bubble-user`, `--session-bubble-user-border`, `--session-slate-soft`, `--session-button-inset`, `--session-button-inset-strong`, `--session-drawer-bg`, `--session-bubble-shadow`, `--session-plate-shadow`, `--session-sheet-shadow`, `--session-walnut-meta-strong`, `--session-walnut-meta-soft`, `--session-walnut-highlight`, `--session-ink-user-text`, `--session-error-border`, `--session-error-border-soft`, `--session-oauth-surface`, `--session-oauth-border`. Components refactored: `Bubble` (jove + user border, shadow, user text color), `Plate` (border, shadow, eyebrow), `TopBar` (button inset), `Modal` (border + shadow + destructive border), `ConnectionErrorPlate`, `BetaFeedbackButton` (popover border + shadow), `ChatInput` (pill border + shadow + mic/stop button bg), `SessionDrawer` (panel bg + close button), `MobileSession` (chip cards + icon bg + guided intake card), `MobileManual` (export sheet), `MobileSettings` (SectionHeader + danger border), `EmptyLayer` + `PopulatedLayer` (header colors), `InfoScreens` (plate), `LoginScreen` + `AuthPromptModal` (Google OAuth wash now its own `--session-oauth-*` token pair since the cream-on-dark rgba didn't map cleanly to existing walnut tokens). **Settings toggle (Phase 5).** New `AppearanceToggle` (`src/components/shared/AppearanceToggle.tsx`) — three-state segmented pill (System / Light / Dark) reading `useTheme()`, active segment uses `--session-walnut-surface` bg + `--session-ink` text + optional card shadow, inactive segments transparent with `--session-ink-mid`. Inserted into `MobileSettings.tsx` between the Account and Crisis Support sections as a `SectionHeader` + `SettingsRow` with a "Theme" label and "System follows OS preference" mono-caps subtitle. **Cross-mode visual audit (Phase 6).** Walked every authenticated surface in both modes at 410×840: welcome chat, drawer (open + closed), Manual list, Manual entry expanded, Settings, ConfirmationModal. Confirmed via `getComputedStyle` probe that no white text (`rgb(255,255,255)`) leaks into the light `<main>` and no near-black backgrounds leak in. Gradient stacks verified against the V3 demo `C` object. Dark mode re-walked to confirm no regressions. **Tests + build (Phase 7).** 626 tests pass. `npm run build` clean. PWA `theme-color` meta flips between `#0A0B10` (dark) and `#E5D8BE` (light). FOUC script verified across reload — chosen theme persists and renders correctly on first paint. **Default behavior.** New users follow OS preference (existing dark users keep their experience because dark is the fallback when prefers-color-scheme is dark or unset). Explicit selection via the toggle overrides system; clearing the setting falls back to system. **What's deferred.** Manual layer cards keep the dark-mode treatment of border-only without `--session-card-shadow` lift even though the demo's PaperRow uses `var(--card-shadow)` — left as-is since both modes render correctly and adding shadow now would change dark mode's shipped look. **Desktop colophon toggle (post-merge).** Added an inline `THEME · SYSTEM · LIGHT · DARK` row to `DesktopVitrine.tsx` colophon, sitting above the "In closed beta · v… · Privacy · Terms" meta row. Editorial register matching the meta line: mono caps, dot separators, active option in `var(--session-ink)` with a 1px underline at 4px offset, inactive options inherit ink-mid. Hidden on mobile (the colophon is already gated by `.mw-desktop-paratext`). Mirrors the mobile Settings → Appearance toggle, both reading the same `useTheme` hook. **Result.** 6 commits on `claude/light-mode-implementation` (theme infrastructure → tokens → per-surface gradients → component-level tokenization → settings toggle → desktop colophon toggle). Dark mode untouched. See [implementation plan](implementation/light-mode.md) for the full files-touched map.
- Dark-mode redesign (2026-05-12, fervent-austin branch, all 9 phases): Full visual redesign of every authenticated and unauthenticated surface against `docs/implementation/dark-mode.md` and `src/app/demo/glass/page.tsx`. **Foundation (Phase 1).** `globals.css` `:root` rewritten with walnut+slate dark-only tokens — `--session-linen: #0A0B10`, `--session-walnut: rgb(170,120,82)`, `--session-walnut-surface: rgba(115,72,42,0.20)`, `--session-walnut-border: rgba(170,120,82,0.18)`. Surface gradient tokens added: `--session-bg-welcome`, `--session-bg-chat`, `--session-bg-checkpoint`, `--session-bg-manual` — each a stack of 3–4 radial gradients placing walnut warmth in different corners. Spectral added via `next/font/google`, exposed as `--font-spectral`. `ThemeInit.tsx`, `useTheme.ts`, the FOUC script, `data-theme` attribute, and all theme-toggle UI deleted. **Shared primitives (Phase 2).** `<Bubble />` for side-anchored chat (Jove left 92%, walnut surface, sage JOVE label / user right 88%, slate surface, asymmetric 5px speaker-side corner). `<Plate />` for walnut-tinted cards with eyebrow + 24px Spectral heading + 17px body. `<TopBar />` for the persistent `mywalnut.` masthead with optional back and menu glyphs. **Chat surface (Phase 3).** `MobileSession.tsx` rebuilt: italic-on-page chat → `<Bubble />`, checkpoint card → `<Plate />`, typing indicator → single pulsing fleuron in a Jove bubble, header → `<TopBar />`. `ChatInput.tsx` restyled as a walnut pill composer (`borderRadius: 22px`, walnut-surface bg, glass blur). TextBtn mono-caps "SEND ›" pattern; mic recasts to 28px ink-ghost glyph. **Manual surface (Phase 4).** `MobileManual.tsx` simplified to flat expanding-cards document with walnut glass section cards and chevron expand/collapse. `EntryItem.tsx`, `PopulatedLayer.tsx`, `EmptyLayer.tsx` restyled. Drop cap and "featured Layer I" removed; all five layers render equally. **Navigation rework (Phase 5).** `MobileNav.tsx` deleted. `MobileLayout.tsx` drops the bottom tab bar; chat extends to safe-area-inset-bottom. `SessionDrawer.tsx` expanded to be the full app menu: new-session pill, sessions list, primary nav rows (Read my Manual with entry count, Settings, Beta feedback), oxblood Crisis support footer. `MainApp.tsx` `activeTab` → `activeView` with drawer-driven view switching; owns `drawerOpen` + `feedbackOpen` state. `BetaFeedbackButton` refactored to a controlled walnut glass popover, no longer rendering its own fixed-position trigger. **Onboarding + auth (Phase 6).** `EntryScreen.tsx` collapsed from 655-line responsive landing to centered hero (72px wordmark, italic Spectral thesis, walnut pill BEGIN, Sign in link). `LoginScreen.tsx` rebuilt with walnut glass plate forms across login/signup/forgot modes — mono-caps labels, walnut-border inputs, TextBtn SIGN IN/CREATE ACCOUNT/SEND RESET LINK CTAs, Google OAuth preserved. `InfoScreens.tsx` collapsed to single disclaimer card on walnut plate. `SeedScreen.tsx` restyled with TopBar, walnut checkbox, TextBtn Begin. `OnboardingFlow.tsx` wires Begin → signup routing and applies welcome gradient background. **Edge cases (Phase 8).** Surface gradients applied via CSS vars (chat → `--session-bg-chat`, manual/settings → `--session-bg-manual`, all onboarding → `--session-bg-welcome`). DesktopVitrine masthead period color fixed from sage to walnut. SW update prompt repositioned (no longer offset for the gone tab bar). Public pages (privacy/terms/waitlist) inherit the dark palette automatically through the shared tokens. **Design audit pass (post-Phase-9).** Two follow-up commits after the initial ship: (a) string-level alignment — TopBar wordmark now CENTERED in a 3-slot flex layout (back chevron / wordmark / menu) with empty 30×30 spacers for missing actions; MobileManual added an `N entries` walnut mono-caps eyebrow above `Your Manual.`; PopulatedLayer + EmptyLayer headers restyled to `Layer I · Name` mono-caps walnut-light with right-aligned numeric count and walnut-border-soft bottom rule (roman numerals derived from layer id); EntryItem headlines got a walnut-period suffix and "Explore further" became a mono-caps walnut TextBtn with chevron + walnut underline; ChatInput mic/stop wrapped in 32px circular `rgba(0,0,0,0.40)` surfaces with 14px / 1.7 stroke icons. (b) computed-style alignment — read `getComputedStyle().backgroundImage` at 410×840 against every demo Frame; gradient strings matched byte-for-byte for Welcome/Chat/Manual; two real deltas surfaced: Bubble Jove + Plate border alpha was 0.18 (from `--session-walnut-border` token) vs the demo's literal 0.20, fixed with inline `rgba(170,120,82,0.20)` (token's 0.18 intentionally preserved for lighter divider roles in SessionDrawer + Manual cards); `--session-bg-checkpoint` was defined but never wired — MobileLayout now accepts `hasActiveCheckpoint?: boolean` and swaps the session panel `backgroundImage` to `var(--session-bg-checkpoint)` (4-stop walnut-top + slate-bottom stack) when MainApp's `activeCheckpoint !== null`. Verified live via DOM probe: `--session-bg-checkpoint` resolves to the demo's exact 4-stop value byte-for-byte. **Open / deferred.** Phase 7 (modal restyle for 5 modals + MobileSettings flat-list refactor) skipped — modals still on pre-redesign styling. Welcome-vs-Chat gradient routing for the chat-empty new-user state — demo uses `bgWelcome` for the "Hello, I'm Jove" hero, production keeps `bgChat`; left as a product decision. Radial-gradient positions tuned for the demo's 410-wide container; real iPhone widths (390 / 393) will see slight position drift — noted as tuning, not bug. **Post-ship UI fixes (post-merge).** Five issues surfaced by the project owner on production: (1) feedback affordance lost in Phase 5 — reverted `BetaFeedbackButton` to self-managing trigger (persistent mono-caps "feedback" pill top-right of phone frame, internal popover state + click-outside close); removed the drawer's "Beta feedback" row and `MainApp.feedbackOpen` state; moved render back inside `MobileLayout`. (2) hamburger menu moved to LEFT slot — `TopBar` now puts `onMenu` (or `onBack` as fallback) in the LEFT, wordmark centered, RIGHT slot empty (frees the corner for the feedback button to float above). (3) `ChatInput` redesigned to match the demo `PillComposer` spec exactly — `borderRadius: 22px → 999` (full pill), `padding: 6px 16px → 10px 14px`, `backdropFilter: blur(20px) → blur(32px) saturate(150%)`, added `boxShadow: 0 12px 40px rgba(0,0,0,0.40)`; placeholder repositioned from `left: 0` to `left: 14px` so the italic hint aligns with the textarea's content start (was offset by the pill's padding-left); placeholder fontSize `17 → 15px` to match demo's quieter hint register. (4) `Bubble` bottom padding `20px → 14px` — lighter visual weight below the last line of text. (5) tab-bar blank space removed — `MobileSession` + `MobileManual` scroll containers dropped the leftover `paddingBottom: "calc(52px + env(safe-area-inset-bottom))"` (MobileNav clearance that no longer applied); now just the safe-area inset. **Phase 7 + Phase 8-tail follow-ups (post-merge).** Closes the remaining audit gaps with a final pass. **(a) Shared <Modal /> primitive** at `src/components/shared/Modal.tsx` — walnut-glass plate, backdrop, body-scroll lock, focus return, Escape to close, destructive variant. All five modals (`FirstCheckpointModal`, `PatternFormingModal`, `ChatWindowModal`, `AuthPromptModal`, `ConfirmationModal`) restyled to use it: walnut-meta eyebrow, Spectral heading with walnut period, paragraph body Spectral 15.5px, italic cancel + mono-caps TextBtn confirm; `ConfirmationModal` adds an oxblood-accented destructive variant. Test files updated to read the shared `Modal.tsx` for role/aria-modal assertions. **(b) MobileSettings flat-list** — `SectionHeader` is now a typographic h2 (mono-caps walnut-light, bottom rule, oxblood for Crisis), no accordion toggle; `openSections` state and `toggleSection` function deleted. All sections render simultaneously. **(c) `ConnectionErrorPlate`** at `src/components/shared/ConnectionErrorPlate.tsx` — quiet walnut plate over chat with oxblood "Connection lost" eyebrow, Spectral body, Retry TextBtn + italic dismiss. Replaces the inline oxblood top-rule. **(d) Composing state** — when `checkpointActionState === "confirmed"`, the receipt swaps to a pulsing sage fleuron + italic Spectral "Putting it on the page…"; after the entry lands and the historical render kicks in, the receipt shows "Saved to Layer Two" walnut mono-caps (ordinal derived from `LAYER_ORDINAL`). **(e) Empty Manual placeholder** — italic Spectral line "Your Manual fills as you and Jove find patterns together." above the layer headers when no entries exist. **(f) Welcome chips → walnut cards** — the three `WELCOME_CHIPS` now render as glass cards with a leading walnut chevron, italic Spectral chip text, blur + shadow; the "Help me get started" link became a styled Guided intake card with title + sub. **(g) Explore-further chip** — walnut pill at top of chat surfaces the entry the user tapped Explore Further on (mono-caps `LAYER N · LayerName` eyebrow + Spectral entry name + dismiss ✕). `MainApp` tracks `currentExploration` in state, snapshots from the `handleExploreWithPersona` callback, and clears when the conversation switches. **Desktop layout audit + fixes (post-merge).** Five issues from a 1280×900 viewport audit of onboarding/login/Settings: (a) `EntryScreen` rendered edge-to-edge at desktop widths (no max-width container, BEGIN pill full-bleed, brittle `top: 38%` pin). Rewritten with a responsive `.mw-entry-column` (max-width 560→600→640px), `flex justify-content: center` on the wrapper for true vertical centering, hero wordmark scaled `72→88→96→120px` across mobile/tablet/desktop/wide, BEGIN pill capped at `maxWidth: 320px`, thesis `max-width: 44ch`. (b) Duplicate wordmark on every wrapped surface — outer DesktopVitrine masthead AND in-frame TopBar wordmark both showed at ≥1030px. TopBar wordmark span gets `className="mw-topbar-wordmark"`; globals.css adds `@media (min-width: 1030px) { .mw-phone-frame .mw-topbar-wordmark { visibility: hidden; } }` (visibility, not display, so flex layout slot stays — back/menu/feedback positions stable). Below 1030px the outer masthead is hidden and the in-frame wordmark remains. (c) `LoginScreen` content scroll container gets `justifyContent: center` — forms balance in the phone-frame's 932px desktop height instead of pinning to the top. (d) `SeedScreen` drops the bottom-pinning `flex: 1` spacer for a flex-centered content area (the demo's "pin to bottom" pattern fell to the floor at desktop frame heights). (e) `SeedScreen` disabled Begin border-bottom changes from `--session-ink-whisper` (invisible on the walnut gradient) to `--session-walnut-border` (warm + visible). **Result.** 12 commits across the branch (Phases 1, 2, 3, 4, 6, 5, 8+9, two audit fixes, post-ship fixes, Phase 7 follow-ups, then desktop layout fixes); 626 tests pass; build clean; no console errors. Demo at `/demo/glass` remains as a frozen visual reference. See [implementation plan](implementation/dark-mode.md) for the full files-touched map.
- Guided intake instrumentation (2026-05-07, pedantic-hofstadter branch, session 4): PostHog event coverage so the team can measure guided intake performance from the dashboard alone — no DB queries, no transcript reads. **Four questions answered.** (1) Fraction of conversations started in guided-intake: `conversation_started.entry_point` (union expanded to `"situation" | "guided-intake"`). (2) Turn count to first checkpoint: new `user_turn_count` property on `checkpoint_proposed` counts user messages at fire time (cleaner than deriving from `message_number`, which counts assistant turns). (3) Fraction of guided-intake checkpoints accepted/rejected/uncommitted: `mode` property added to `checkpoint_proposed`, `checkpoint_confirmed`, `checkpoint_rejected`, `checkpoint_refined`, `checkpoint_deferred`, `conversation_ended` — uncommitted is derivable from PostHog primitives. (4) Which opener-flow path fired: new `guided_intake_opener_fired` event with `variant ∈ {default, recency_drop, moments_to_states, gentle_end}`, detected client-side by string-matching the assistant turn against the canonical phrases in `system-prompt.ts`. **Plumbing.** Server emits `mode` in every `message_complete` SSE event (`call-persona.ts`); client mirrors mode in a ref and attaches it at fire time. No new server-side PostHog dependency — all events fire from the browser, matching the existing 11-event Stage 1 pattern. PII guard tests updated. **Tests.** 41 new (589 → 630): variant detector unit tests (12, including smart-quote and case-insensitive normalization), wiring assertions on events.ts/useChat.ts/call-persona.ts/sse-parser.ts (16), `user_turn_count` semantics (4 — counts only role==='user'), updated PII guard cases. **Deferred.** No dedicated `no_commit` event — `conversation_ended` fires only on explicit `startNewSession()`, not tab-close; tab-close detection would require a separate session-end signal that doesn't exist today. Q3's "uncommitted" is still answerable by joining `checkpoint_proposed` with the absence of a subsequent `checkpoint_confirmed` before the next `conversation_started`. Build ✓, 630 tests ✓.
- Style-guide alignment of chat surface (2026-05-07, naughty-jones branch, third pass): Five fixes after a three-agent audit against the project's authoritative style guide (now committed to `docs/reference/style-guide/` as 17 sg-*.jsx files extracted from the bundled SG artifact). **(1) JOVE label.** Now uppercase via `textTransform`, letterSpacing 2.2, full `--session-persona` color — matching `sg-chat.jsx` §05.1 micro-caps spec. Was lowercase, 1.5px tracking, `--persona-soft` (half-strength). The redundant `.toUpperCase()` calls at MobileSession.tsx:778 and 883 were dropped since CSS now handles the casing. **(2) Desktop masthead.** `DesktopVitrine.tsx` masthead changed from "my walnut" (with space, no period) to "mywalnut." (no space, sage period) — now matches the mobile chat header wordmark and `sg-tokens.jsx::Wordmark`. **(3) Composer chrome.** ChatInput restructured: `borderTop: 1px solid var(--session-hair)` above the row (per SG §05.5), replacing the previous bottom inset shadow. Send button replaced with the SG TextBtn pattern — "SEND ›" mono-caps text-link with hairline rule beneath, appearing only when text is present. The 32px sage-filled circle with white arrow — bubble chrome the SG explicitly forbids — is gone. The mic icon stays for voice (a mywalnut-specific feature the SG doesn't sanction directly) but recasts to a 28px ink-ghost glyph at the SG expand-glyph register, not a 44px sage circle. **(4) Typing indicator.** Three pulsing dots replaced with a single sage fleuron (`❦`) pulsing — per `sg-chat.jsx` §05.1: a typographic ornament marks "Jove is composing," not speech-bubble dots. **(5) MobileNav active state.** Replaced ink underline with a 3px sage dot below mono-caps. Sage now appears on the bottom bar as the canonical "you are here" cue per `sg-nav.jsx` + `sg-donts.jsx` §94 ("sage is the one thing per screen"). Inactive labels stay at `--session-ink-mid`; active labels lift to `--session-ink` plus the dot. **Plus:** style-guide reference files committed at `docs/reference/style-guide/` (sg-tokens, sg-chat, sg-nav, sg-buttons, sg-foundations, sg-shell, sg-dark, sg-a11y, sg-component-states, sg-donts, sg-inputs, sg-lists, sg-long-content, sg-overlays, sg-plates, sg-states, sg-app) so future agents can audit against the canonical spec. **Two deliberate divergences left in place** (per user direction in the same session): user text opacity at 0.68 light / 0.70 dark (SG: ink85 = 0.82 — user wanted "a touch lighter"); user message `marginLeft: var(--sp-xl)` 56px (SG keeps user in same column as Jove, distinguished only by rail+italic — user wanted clearer "user side"). 564 tests pass. Build ✓. See [merge commit 1dd2550](https://github.com/h2onation/mantle/commit/1dd2550).
- Guided intake mode (2026-05-07, pedantic-hofstadter branch, 3 sessions): Full pipeline for a new "guided-intake" conversation mode where Jove sends the opening message instead of waiting for the user. **(1) Prompt module.** New GUIDED INTAKE block in `system-prompt.ts` (Tier 3, conditional on `mode === "guided-intake"`). Contains an opener prompt, a deepening rhythm, five layer-probing questions, and a readiness gate. The opener text lives in a shared constant (`GUIDED_INTAKE_OPENER` in `src/lib/persona/guided-intake-copy.ts`) imported by the prompt builder. **(2) Mode column.** Migration `20260507000000_conversation_mode.sql` adds a `mode TEXT DEFAULT 'situation'` column to `conversations` with a CHECK constraint (`situation`, `guided-intake`). API route validates mode on create. Persona pipeline reads mode from the conversation row and passes it through to the prompt builder. **(3) Frontend affordance.** "Help me get started" text link appears in all three empty-conversation states in `MobileSession.tsx` (first-time user, returning user, fallback). Click triggers `startGuidedIntake()` in `useChat.ts`, which POSTs to `/api/chat` with `{ message: null, mode: "guided-intake" }` — reuses the existing `triggerPersonaOpener` pattern to have Jove speak first. Guards: isLoading/isStreaming, messages.length > 0. **(4) Tests.** 29 new tests across 4 files: system prompt rendering (6), API mode validation (4), pipeline mode integration (3), opener constant + UI wiring (12). All pass. **Deployment note:** the `conversations.mode` column migration was applied to the remote Supabase DB on 2026-05-07 (verified via `supabase migration list` — present on both Local and Remote). Build ✓, 589 tests ✓.
- Theme polish — dark-mode review fixes (2026-05-07, naughty-jones branch, second pass): Seven fixes after a dark-mode design review identified rough edges in the initial Linen v2 ship. **(1) Vignette direction.** New `--session-vignette` token (`rgba(22,20,18,0.06)` light / `rgba(0,0,0,0.35)` dark) replaces the previous use of `--session-hair-soft` for corner darkening in MobileLayout and OnboardingFlow. The hair-soft token is light in dark mode (it's used for borders), so corner gradients were producing a halo (lifting edges) instead of a vignette (darkening edges). Now corners darken correctly in both modes. **(2) Bottom nav lifted.** MobileNav background changed from flat `--session-linen` to `--session-cream` (one step warmer/lighter), so the nav reads as a quiet platform instead of a flat slab. Especially needed in dark mode where the chat above has the new vignette darkening, leaving an un-vignetted nav looking "true black" by comparison. **(3) Input focus state.** ChatInput dropped the top inset hairline; the focus state is carried by a single bottom underline. The earlier two-line "groove" treatment perceptually fused into a 4-side outline in dark mode where hairlines lift more. **(4) User message indent.** User messages now sit at `marginLeft: var(--sp-xl)` (56px) with `paddingLeft: var(--sp-sm)` instead of `paddingLeft: var(--sp-md)` (22px). Their rail starts past Jove's column, creating clear "user side" affordance — marginalia-on-the-right rather than two voices in the same gutter. **(5) Jove voice softened in dark.** `--session-ink-persona` now resolves to `rgba(242,234,214,0.72)` in dark mode (was full `#F2EAD6` matching `--session-ink`). Jove no longer shouts in pure cream against a dark ground. Light mode unchanged. The Jove rail strengthened from `--persona-soft` (sage at 0.32) to full `--session-persona` for clearer differentiation against the muted user rail. **(6) Desktop theme toggle.** DesktopVitrine colophon now exposes `Theme · sys · light · dark` inline with the existing version/Privacy/Terms meta row, with the active option underlined. Same `useTheme` hook as Settings → Appearance on mobile. Wide and narrow desktop both show it. **(7) Settings APPEARANCE expanded by default.** Was hidden behind a `▾` accordion next to ACCOUNT; now expanded on first load so the SYSTEM/LIGHT/DARK toggle is visible without a click. Plus: `--session-glow-scroll` lowered from 0.10 to 0.04 in dark mode so the top-of-chat fade reads as a gradient rather than a sage stripe. Verified in browser at mobile + 1280×800 desktop in both color schemes; theme round-trip works (system → light → dark → system) with no flash, no hydration errors, no console errors. 564 tests pass. Build ✓. See [merge commit 68c30fe](https://github.com/h2onation/mantle/commit/68c30fe).
- Desktop canvas polish — texture + frame shadow on vitrine (2026-05-07, cool-goldstine branch): Two CSS-only changes to `DesktopVitrine.tsx` so the desktop canvas reads as continuous material rather than a phone floating on a flat surface. **(1) Canvas texture.** The outer parchment canvas now carries the same paper-surface treatment as the phone interior panels — fractal noise SVG at 1.2% opacity (half the 2.5% used inside the phone) plus a radial vignette darkening 2% at the far corners (half the 4% inside). Reduces the visual gap between the frame interior and the surrounding surface. **(2) Frame shadow.** Phone frame border dimmed from 8% to `var(--session-hair-soft)` (uses the new Linen v2 token) and a soft diffused `box-shadow: 0 0 80px rgba(26,22,20,0.06)` added. Shadow reads as depth (page lifts from folio); hard border alone read as device edge. Both changes are invisible on mobile — the `clamp()` border trick resolves to 0px at ≤430px, and the canvas is behind the full-viewport phone frame. Build ✓, 564 tests ✓.
- Theme system — Linen Design System v2 + system-driven dark mode (2026-05-07, naughty-jones branch): Two interlocking changes shipped together. **(1) Linen v2 token rewrite.** Warm bone canvas (`--session-linen: #FAF7F0`) replaces the prior gray-white. Sage shifts from `#556B48` to `#5C6B4E` and gains soft/muted/border/tint variants. Oxblood error (`#7A3B2E`) replaces the prior brick red, and a new amber warning role (`#9A6B2A`) is added for quiet heads-ups. New literary spacing scale (`--sp-hair` through `--sp-xxl`, named not numeric), almost-flat radii (`--radius-none/xs/sm/pill`), and a paper-lift elevation pattern (used sparingly). MobileSession's chip rail is restyled as italic serif text-with-rule links instead of pilled cards; the masthead wordmark becomes "mywalnut." with a sage period; checkpoint card actions restructure into a title-case mono-caps primary button ("Put it in my Manual" / "Put it in as it is") plus lowercase italic serif secondary actions ("close but not quite", "this is not me", "let it go"). All hardcoded hex/rgba values across components were replaced with CSS custom properties so the system adapts cleanly. **(2) Dark mode.** System-driven by default with explicit override. An inline FOUC-prevention `<script>` in `<head>` reads `mw-theme` from localStorage before paint, sets the `data-theme` attribute on `<html>`, and updates the `theme-color` meta. Three-state toggle (system / light / dark) lives in Settings → Appearance, with a mono-caps text-with-rule pattern matching the rest of the design system. New `useTheme` hook (`src/lib/hooks/useTheme.ts`) uses `useSyncExternalStore` to track preference and resolved values, listens to `matchMedia('(prefers-color-scheme: dark)')` for system changes, and dispatches DOM updates on every preference change. New `ThemeInit` component mounts at the layout root and is a no-render wrapper around the hook. `suppressHydrationWarning` on `<html>` prevents the React mismatch from the FOUC script setting attributes before hydration. Dark tokens are LIFTED for legibility on dark ground rather than simply inverted: linen → `#15110C`, ink → `#F2EAD6`, sage → `#9CB18A` (lifted from `#5C6B4E`), oxblood → `#D08278` (lifted from `#7A3B2E`). `--lift` shadow uses `rgba(0,0,0,0.20/0.25)` in dark vs subtle warm shadows in light. **Verification.** Tested in browser preview at mobile and desktop viewports, both color schemes, with theme toggle round-trip (system → light → dark → system). No flash on load, no hydration errors, no console errors. Build ✓, 564 tests ✓. **Test file revisions.** `src/components/onboarding/onboarding-copy.test.ts` updated to expect the new lowercase secondary button labels (`close but not quite`, `this is not me`, `let it go`). Older title-case assertions were treated as outdated since those buttons are now intentionally rendered in lowercase italic serif (literal lowercase, not via text-transform). Primary button assertions (title-case mono-caps "Put it in my Manual" / "Put it in as it is") preserved. See [merge commit 2a8b595](https://github.com/h2onation/mantle/commit/2a8b595).
- Landing — rail-and-numeral architecture, full rewrite from scratch (2026-04-22, page-design branch): Fourth landing pass today, treating the prior file as empty rather than iterating on it. Design direction: the page reads as a small composed document — quiet, premium, confident — but without costume (no Issue-One paratext, no chapter titles, no colophon, no pull-quote, no fleuron). **The one formal gesture** is a thin sage vertical hairline spanning the whole page (canvas `::before` linear-gradient, 1px, fades in at 72px / out at bottom-160px), with hanging markers in the left rail: italic Instrument Serif roman numerals (I–V) for the five Manual layers, JetBrains Mono two-digit numerals (01 / 02 / 03) for the method steps. The rail gives the composition architecture; the markers tie the spine to content without announcement. **Structure, in reading order.** (1) **Masthead** — wordmark "my walnut" + sage period top-left, mono-caps "LOG IN" top-right as a text button (not a link) that fires `onLogin`. Flex justify-between spans the full canvas width, so the wordmark anchors the left edge while the content column below indents past the rail. (2) **Hero** — sage mono eyebrow "mywalnut · a private manual", then headline "A private manual of how you work." at 38 / 68 / 92 / 104px (mobile → ≥1440) in Newsreader 400 with `-0.028em` to `-0.036em` tracking, ink color, sage terminal period. Three-sentence subhead in Newsreader 400 / ink-mid ("my walnut is an AI that helps you write one — through conversation. Nothing enters unless you confirm it. Built for neurodivergent adults."). Outlined CTA `<a href="/waitlist">Join the waitlist →</a>` in ink — ghost button that inverts to ink-filled on hover with a 5px arrow nudge. (3) **Sample Manual entry** — sage mono label "AN ENTRY", ink-faded italic layer marker "from Layer Two — How I process things", italic first-person statement set in Instrument Serif ("When plans shift without warning, my voice is the first thing that goes quiet.") at 25 / 32 / 38px, prose elaboration in Newsreader ("Not because I have nothing to say — but because speech is where my regulation leaves."). (4) **Five layers** — mono-caps kicker "Your Manual, in five layers", then I–V verbatim layer names. The II layer ("How I process things") has a `text-decoration: underline` with sage-border color and 5px offset — a quiet visual tie to the sample entry above. (5) **Method** — three steps with 01 / 02 / 03 mono numerals hanging in the rail, ink-solid lead phrase, ink-mid elaboration. (6) **Final CTA + login + footer** — same ghost CTA, "Already have access? Log in." with inline button, mono-caps `PRIVACY · TERMS`. **Typography.** Newsreader throughout for body, Instrument Serif italic reserved for the sample entry's quoted statement and the roman numerals, JetBrains Mono for eyebrow/kicker/numeral/footer labels only. **Color.** Linen background, ink for content, `--session-persona` (sage) for period marks, eyebrow, roman numerals, mono kicker on the sample label, and the hairline rail. No filled bright buttons, no paper-noise texture, no stock photography. **Motion.** Page-load stagger on hero elements (`mwEntryRise`, 720ms fade-up with 80/200/320/440ms delays on eyebrow/headline/subhead/CTA). No scroll triggers, no rotating specimen. `prefers-reduced-motion: reduce` zeros animation. **Responsive.** Four breakpoints with explicit CSS custom properties (`--mw-gutter`, `--mw-rail`, `--mw-column`, `--mw-section-pad`): mobile 20/40/— content-width-driven, tablet 48/96/600, desktop 64/120/720, wide 80/144/760. Canvas is the column + rail, centered — the rail indent creates a deliberate right-shift of content that reads as editorial composition. **Deleted from earlier pass.** `MYWALNUT · A PRIVATE MANUAL` as a standalone paratext masthead, the rotating specimen, the sand-ripples image (already gone), the single-rule sample sidebar. **Tests.** `src/components/onboarding/onboarding-copy.test.ts` revised: positive assertions cover the new sample-label framing, the I–V roman numerals + 01/02/03 method numerals at JSX-line granularity, the masthead Log in affordance alongside the final one, and the four-breakpoint media queries. Negative assertions preserved from prior pass. 48 EntryScreen assertions, 564 total tests pass. Build ✓.
- Landing — clear/premium pass, no costume, sample Manual entry as visual anchor (2026-04-22, design-3.0 branch merged again): Third landing pass in a single day. The earlier editorial-pastiche direction (Issue-One paratext, "On bringing / On listening / On composing / On sharing" chapter titles, centered pull-quote, sand-ripples photograph, rotating thesis specimen, colophon signature line) was all borrowed costume — conventions lifted from literary publications that earned them through different means. Composition looked editorial but didn't do editorial work. Scrapped entirely. Current page carries itself with type, space, and **one product-specific anchor**. Seven moments: (1) **Wordmark** top-left, 19–24px, "my walnut" with sage period. No rule, no paratext. (2) **Hero** — one headline "A private manual of how you work." at 40 / 64 / 88 / 96px (mobile → ≥1440), one three-sentence subhead ("my walnut is an AI that helps you write one — through conversation. Nothing enters unless you confirm it. Built for neurodivergent adults."), one outlined CTA button ("Join the waitlist →") that inverts to ink-filled on hover with a 4px arrow nudge. No filled sage plate (tech-startup grammar), no text-only italic (too precious) — a confident middle path. Hero is centered. (3) **Sample Manual entry** — the product, as type. Left sage rule, mono-caps kicker "HOW I PROCESS THINGS", italic first-person statement in quotes ("When plans shift without warning, my voice is the first thing that goes quiet."), prose elaboration ("Not because I have nothing to say — but because speech is where my regulation leaves."). This single artifact communicates what a Manual IS in one glance. Replaces the sand-ripples photograph, the rotating thesis specimen, and the HeroManualVignette checkpoint-narrative animation all at once. (4) **How it works** — three plain numbered method steps with small sage italic numerals (1/2/3), lead phrase in ink-solid ("Talk to Jove about things on your mind." etc.), elaboration in ink-mid. No roman numerals, no gerund chapter titles. (5) **Five Manual layers** — literal list of `Some of my patterns / How I process things / What helps / How I show up with people / Where I'm strong`, under a mono-caps kicker "Your Manual, in five layers". Shows the product's actual surface area to a first-time visitor. (6) **Final CTA** — same outlined button + "Already have access? Log in." italic text link. (7) **Legal** — mono-caps `Privacy · Terms`. **Typography:** Newsreader throughout (Instrument Serif unchanged elsewhere in the app), JetBrains Mono reserved for the two kicker labels and the legal row. Sage (`--session-persona`) reserved for period marks, the rule on the sample entry, and the method numerals — otherwise the page is pure ink-on-linen. **Motion:** headline/subhead/CTA fade up on load (120/260/400ms staggered); no rotation, no scroll triggers, no hover tricks beyond the arrow nudge. `prefers-reduced-motion: reduce` zeros every duration. **No paper-noise SVG background** (earlier passes stacked a 3.5% fractal noise texture + a radial vignette; both gone). Flat linen background — the type does the work. **Deleted:** `public/images/hero-sand.jpg` and `.webp` (sand-ripples atmospheric band is gone from both the markup and the repo), `HeroManualVignette.tsx` had already been deleted in the previous pass. **Tests:** `src/components/onboarding/onboarding-copy.test.ts` inverted all the editorial-pastiche assertions — now asserts the sample Manual entry ships, the three method steps use plain prose, the five layers list renders, and negative-asserts ROTATING_EXAMPLES, "Issue One", "On bringing", "Nothing enters the manual" (old pull-quote), "Set in Newsreader" (old colophon), HeroManualVignette imports, and hero-sand image references. 46 assertions, 562 total tests pass. Build ✓. See [merge commit 0c76b99](https://github.com/h2onation/mantle/commit/0c76b99); earlier landing passes today are superseded in source but remain in the commit history ([52f8120](https://github.com/h2onation/mantle/commit/52f8120) editorial-pastiche, [14ba466](https://github.com/h2onation/mantle/commit/14ba466) vignette).
- Landing — editorial periodical direction, rotating thesis specimen, no tech-demo grammar (2026-04-22, design-3.0 branch merged twice): Successor to the earlier same-day "HeroManualVignette" ship, which was reverted once on-screen. The animated checkpoint demo was the right instinct (show the mechanic) but the wrong venue (the hero is for thesis, not demo) — editorial landing pages state, they don't demo. The whole page was restaged as a thin publication; the rotating thesis sentences were restored unframed. **Seven sections, top-to-bottom.** (1) **Masthead** — wordmark "my walnut." with a sage-colored period, alongside two-line mono-caps paratext ("ISSUE ONE · SPRING 2026 / A MANUAL FOR NEURODIVERGENT ADULTS"). On desktop they sit split left/right with a hairline rule drawing across the width on mount; on mobile they stack centered. The old "Built for neurodivergent adults." subhead is absorbed into the paratext so the hero doesn't carry redundant framing. (2) **Hero** — display headline "A more complete manual / of how your mind / works." broken across three lines with manual `<br>`s, pushed to 108px on desktop and 136px at ≥1440; the sage period at the end is the seal. Below, the ten original thesis sentences (`ROTATING_EXAMPLES` in `src/components/onboarding/EntryScreen.tsx`) rotate one at a time in italic Newsreader with a sage bullet prefix. 6.5s dwell (up from 4.5s) and a 520ms crossfade — each sentence reads like a photograph. Unframed: no card, no "IN THE MANUAL" kicker. (3) **Sand-ripples atmospheric band** — unchanged from the earlier ship. Full-bleed editorial photograph at `/public/images/hero-sand.jpg` (572KB, 1600px max; `.webp` source also committed), edges soft-faded into the linen via `mask-image: linear-gradient`, grain overlay at 14% opacity / `mix-blend-mode: multiply`. Heights: 200px mobile, 260px tablet, 360px desktop. (4) **Method** — four beats restaged as chapters. Roman numerals (`i.`, `ii.`, `iii.`, `iv.`) sit in the left margin on desktop (120px column with `position: absolute`), stacked inline on mobile/tablet. Each chapter opens with an italic title — *On bringing*, *On listening*, *On composing*, *On sharing* — above the prose body. The old "An oversimplification of how it works:" heading is gone; chapters don't need a signpost. Beats separated by a single hairline. (5) **Pull-quote chapter break** — "Nothing enters the manual / unless you confirm it." at 54px italic on desktop (64px at ≥1440), centered, floating in ~176px of whitespace. The product's promise elevated to a statement. (6) **CTA** — text-only. An italic intro line ("A manual waits to be written."), then a 48–56px italic "Join the waitlist →" whose arrow shifts right 8px on hover. The filled sage-green `<a>` plate is gone — it was the one piece of tech-startup grammar on the page and imported the wrong register. The text-link treatment matches editorial landing pages in the segment (Aesop, Le Labo, Commune, Rizzoli). (7) **Footer** — fleuron (❦) above an italic colophon line ("Set in Newsreader. Printed on linen. Assembled in conversation.") that signs the document, above the mono-caps Privacy · Terms legal row. **Motion.** One orchestrated page-load reveal (masthead fade, rule draw, headline fade-up, specimen fade-in) spanning ~1s total. The rotating specimen is the only continuing animation. `prefers-reduced-motion: reduce` zeros all durations and forces the rule to its drawn state. **Deleted.** `HeroManualVignette.tsx` was removed entirely (it was introduced three commits earlier this same date and reverted); all its markup, keyframes, and the `useMemo` workaround for dev-mode style-block re-emission are gone. `(Coming soon)` qualifiers on method items 3 and 4 stay dropped — the page is present-tense throughout, not a roadmap. **Tests.** `src/components/onboarding/onboarding-copy.test.ts` inverted the HeroManualVignette assertions (now asserts the ten thesis sentences ship and that `HeroManualVignette` is not imported), added assertions for masthead paratext, chapter titles, pull-quote copy, editorial CTA copy, and the colophon line. Full suite 562 passing (from 560 — two net-new assertions). Build ✓, 562 tests ✓. See [merge commit 52f8120](https://github.com/h2onation/mantle/commit/52f8120) for the full diff of this successor ship; [merge commit 14ba466](https://github.com/h2onation/mantle/commit/14ba466) is the earlier vignette ship that was superseded.
- Track A — checkpoint and onboarding revamp (2026-04-22, design-3.0 branch merged): Eight-gate implementation replacing the inline progress-signals model with an explicit modal flow, revising landing + onboarding copy, and restructuring the checkpoint confirm loop. **Modal state machine:** single integer `profiles.modal_progress` (0-3) with idempotent server-side advance via `POST /api/modal-progress`; advance only on user dismissal, fires sequentially across sessions, terminal at 3. `GET /api/modal-progress` returns `{modal_progress, signup_at_ms, is_anonymous}` so anonymous-auth users are suppressed from the flow entirely. Three new modals in `src/components/modals/`: `ChatWindowModal` fires on first chat-window entry when `modal_progress < 1`; `PatternFormingModal` fires when `modal_progress === 1 AND any layer signal >= emerging AND concrete_examples >= 1 AND emerging_pattern_snippet !== null`, with the snippet snapshot into local state on first open so the displayed text stays stable through user reads; `FirstCheckpointModal` fires before the first checkpoint card render when `modal_progress === 2 && activeCheckpoint !== null`, with the card render suppressed while the modal is open. All three: Escape/Tab handling, focus trap, focus restore, body scroll lock with compose-safe restoration. Five new PostHog trackers: `modal_1_shown` / `modal_2_shown` / `modal_3_shown` on render, `modal_flow_completed` on POST-success from Modal 3 (not optimistic — skews funnel metrics otherwise), `first_checkpoint_completed` on first lifetime confirmation. PostHog's implicit distinct_id and `$session_id` are trusted (no duplication in props); only `time_since_signup_ms` is explicit. **EntryScreen revamp:** new copy, new responsive treatment — mobile 28px-padded single column, desktop 880px centered reading column (escapes `DesktopVitrine` on desktop so the landing reads as a real web page instead of a 430px phone-frame column), wordmark scales 13/18/22px across breakpoints, legal footer gains hairline + 480px centered max-width + 12px type on desktop. Rotating examples unchanged. Primary CTA inverted to "Join the waitlist" plate, secondary "Already have access? Log in." italic link. InfoScreens dropped 4→2 paragraphs (daily-for-two-weeks reminder); SeedScreen 3→2. `{PERSONA_NAME}` interpolation preserved across copy. **Checkpoint card + action restructure:** display labels "Put it in my Manual" / "Close but not quite" / "This is not me" (underlying `confirmed/refined/rejected` action values unchanged). New `deferred` action type for the refinement ceiling's "Let it go" — same DB behavior as rejected (`status="rejected"`) but distinct system message `[User let the checkpoint go]` so Jove bypasses the POST-REJECTION fixed line (the user already explained twice what was off). `refinement_count` field on `checkpoint_meta` (JSONB, no migration); `computeInheritedRefinementCount` chain rule: unbroken `refined` status carries count forward, any terminal status resets to 0. Ceiling card UI (inline conditional render, not a new component) fires at `refinement_count >= 2` with two-button fork ("Put it in as it is" → confirmed, "Let it go" → deferred). `trackCheckpointDeferred` analytics separate from `trackCheckpointRejected` for ceiling-rate monitoring. **Post-confirm flow + inline transition:** extended SSE protocol to support multiple `message_complete` events per streaming response; client discriminates bubble vs card via `checkpoint` null/non-null (no new field). `cleanContent` now mandatory on the final `message_complete` because the client resets its text buffer per event. New `prependedMessages` primitive on `callPersona` options (type intentionally has no `checkpoint` field — compile-time guard preventing shortcut checkpoint delivery). First-lifetime confirmation: Message 1 "In. A working name: '[HEADLINE].' Yours to change." server-templated (no LLM call — deterministic, eliminates deviation risk on the stamp), Message 2 LLM-generated via new `postConfirmMode="first-message-2"` system-prompt block. Subsequent confirmation: single LLM message via `postConfirmMode="subsequent-single"` with server-built entries summary (Oxford-comma pluralization in `buildEntriesSummary`). Subsequent checkpoints get an inline transition message ("Something else is forming. Let me put this one in front of you.") inserted as a separate assistant row with `created_at = jovResponse.created_at - 1s` so reload queries return it before the card. Existing `POST-CHECKPOINT` prompt block deleted — replaced by the two mode-specific blocks. `useChat` refactored for multi-event streams (append per event, reset buffer per event, finalize per event). **Post-rejection fixed line** (added Phase 7-Low): `POST-REJECTION` block instructs Jove to respond with exactly "Okay. What made it miss?" on the turn immediately after a rejection, then return to natural exploration. **Returning-user opener tightened:** three-part template — "Welcome back." / reference to recent entry or open thread / "What is on your mind today?" — activated-user carve-out drops both the greeting and the Manual reference. **Gate 8 deletions:** welcome-prose block removed from `MobileSession` (Modal 1 replaces); `PROGRESS SIGNALS` block removed from `system-prompt.ts` (modals replace EARLY FRAME / DEPTH BUILDING / CHECKPOINT APPROACHING); `PROGRESS SIGNAL DELIVERY` section removed from extraction prompt; `early_frame_delivered` / `depth_signal_delivered` / `approaching_signal_delivered` fields removed from `ExtractionState` and all reducer paths. Collateral: the long-standing pre-existing typecheck error in `src/lib/persona/persona-pipeline.test.ts` (fixture missing `observation_miss_count`) resolved during Gate 8 — `npx tsc --noEmit` is clean for the first time since early April. **Deploy order (STRICT, ONE-DIRECTIONAL — SERVER FIRST THEN CLIENT):** the new server can serve both old and new clients, but the new client cannot tolerate old server. Old server emits one `message_complete` per stream; new client resets its buffer on each `message_complete` and expects `cleanContent` on each — against an old server, non-first messages would render empty. Server must deploy first and old clients must roll forward before 7e/7f paths exercise. The `?:` optional fields on the SSE payload (`emergingPatternSnippet`, `hasLayerEmergingOrBeyond`, `concreteExamples`, `checkpoint.refinement_count`) are backward-compatible in one direction only: old clients ignore unknown fields against new server (OK); new clients degrade on missing fields against old server (not OK if multi-event paths fire). **Known gap (out of scope):** text-channel parity for the refinement ceiling and `deferred` action. Text users repeatedly typing NOT QUITE will keep incrementing `refinement_count` server-side, but no text-equivalent of the ceiling UI or defer keyword exists — no text ceiling fires. Acceptable for beta; text channel is not shipping this phase. **Reserved observability flag:** `modalStateLoading` in `MainApp.tsx` distinguishes "modal-progress GET in-flight" from "GET completed with no data"; read side is intentionally unused, setter fires in the fetch's `finally`, documented inline with an `eslint-disable` directive and a rationale comment. Available for future retry/observability logic; if no consumer arrives in a few weeks, next engineer to read the comment can delete safely. **Manual-eval checklist** planned as `docs/track-a-manual-eval.md` — deferred; not authored in this ship. Should be produced before the next meaningful Jove-prompt change so snippet quality / post-confirm compliance can be validated against a reusable conversation set. Build ✓, 560 tests ✓ (from 472 — 88 new across onboarding-copy + system-prompt blocks + extraction parser + events + modals + persona-pipeline helpers + sse-parser), typecheck ✓. See Gate 8 commit for the full diff (31 files changed, 3101 insertions, 574 deletions).
- Extraction pipeline hardening — state wipe, observability, waitUntil (2026-04-21): Three commits closing diagnostic work on intermittent extraction failures observed in Vercel logs on 2026-04-17 (three `DOMException [AbortError]` + one `Anthropic API returned unexpected response shape` over a four-hour window, all under the `[extraction] Failed:` prefix). Extraction is fire-and-forget from both web (`/api/chat`, edge runtime) and text (SMS webhooks, nodejs runtime); it writes `conversations.extraction_state` to feed Jove's next-turn research brief and does not produce Manual entries (those come from `confirmCheckpoint`). **(1) State-wipe fix** (`src/lib/persona/extraction.ts`): catch block used to return `{...state, next_prompt: "", sage_brief: ""}` on failure, which `fireBackgroundExtraction` then wrote to the DB — one failed extraction silently blanked the prior turn's working brief on top of the original error, degrading Jove's next turn. Now re-throws; `fireBackgroundExtraction`'s existing .catch already skips the DB write so the prior good state stays authoritative. The `[extraction] Failed:` log is preserved so existing ops queries match. **(2) Structured observability** (`src/lib/persona/persona-pipeline.ts`, `src/lib/anthropic.ts`): two new structured log lines per extraction attempt — `[persona-pipeline] extraction_attempt {conversation_id, message_count, is_first_checkpoint}` as the failure-rate denominator and `[persona-pipeline] extraction_failed {conversation_id, error_class, error_message, error_name}`. New `classifyExtractionError()` helper buckets into `abort` / `response_shape` / `parse` / `http` / `other` so log queries can group failures by cause instead of grepping free-form error messages. On the "unexpected response shape" path, `anthropicFetch` now emits `[anthropic] unexpected_response_shape` with sanitized payload metadata (keys, type, stop_reason, content_length, content_types, and any Anthropic-framework error_type/error_message fields) before throwing — never logs `content[i].text` per the CLAUDE.md Security Rule against logging user content. Enables measuring the failure rate and seeing a real Error B payload next occurrence. **(3) waitUntil** (`src/lib/persona/persona-pipeline.ts`): wraps the extraction promise in `waitUntil` from `@vercel/functions` so the Vercel platform keeps the function alive until extraction settles. This addresses the dominant failure (Error A, AbortError) — when the parent request's response closes before the Sonnet call returns, Vercel was terminating the in-flight fetch and throwing DOMException. Next.js 14 Route Handlers don't expose a native waitUntil (Next 15's `after()` does); `@vercel/functions` is the canonical shim and works on both edge and nodejs runtimes. Off-Vercel (local dev, tests) it degrades to a plain promise, so no test changes needed. `@vercel/functions@3.4.3` added as a direct dep (was already transitive via posthog-node). **Not done, gated on observation**: tool-use migration for extraction / classifier / confirm-checkpoint — deferred until the new payload log captures another Error B occurrence so the rewrite isn't done blind. Post-merge watch: `error_class: "abort"` counts should drop sharply; `error_class: "response_shape"` firings will pair with the `[anthropic] unexpected_response_shape` line by timestamp and carry the sanitized payload needed to decide on tool use. Build ✓, 472 tests ✓.
- Jove response-structure validator + voice rule 15 + dash/loaded-question exemplars (2026-04-16): Follow-ups from `/evaluate` on a first-session transcript that caught the two recurring drift patterns the prompt wasn't stopping — multiple question marks per turn and em dashes joining clauses — plus a third pattern where Jove was smuggling its hypothesis into questions and getting the user to confirm its own frame. **Validator:** new `validateResponseStructure(content, messageId)` in `src/lib/persona/persona-pipeline.ts` (co-located with `validateComposedEntry`). Two checks: `question_count` (counts `?` characters — 0 or 1 ok for the post-confirmation transition, 2+ is a violation) and `dash_usage` (counts `—` plus spaced en-dash/hyphen sequences matching ` – | - `). Logs structured `console.warn` lines as `[persona-pipeline] response_validation { type, check, count, message_id }`. **Non-blocking for beta** — logs only, does not interrupt conversation flow. Promotes to rewrite/strip only after log volume justifies it. Wired into both persona paths: `call-persona.ts` at step 11a and `persona-bridge.ts` after DB insert. Runs on raw `fullText` not `conversationalText`, because `CRISIS_RESOURCES` contains an em dash that would produce false positives on `dash_usage`. **Voice rule 15:** "Never load a question with the answer you expect. If your hypothesis is inside the question, the user is confirming your frame, not finding their own. Rewrite as an open invitation." Added to `voice-autistic.ts` `VOICE_RULES` (14 → 15); doc comment updated to drop the hardcoded count. `system-prompt.test.ts` assertions use `VOICE_RULES.length` dynamically so no test change required. Count comments bumped in `CLAUDE.md` (`VOICE_RULES (14)` → `(15)`), `docs/rules.md` ("The 14 voice rules" → "The 15 voice rules"), and the `system-prompt.ts` Tier 2 block comment ("14 voice rules" → "voice rules"). **Dash exemplar:** added inline with the Tier 2 VOICE dash rule in `system-prompt.ts` — two bad/good pairs (`"She went quiet — what did you do?"` / `"She went quiet. What did you do?"` and `"Not the wrong thing — the true thing."` / `"Not the wrong thing. The true thing."`). Rule existed but kept getting ignored; pairing bad/good examples next to it makes the period-replacement obvious to the model. **Loaded-question exemplar:** added to DEEPENING weak→strong list: `"Do you feel like everyone else got the manual and you didn't?"` → `"What happens when you realize you didn't know the code?"`. The bad form packs Jove's read into the question (eval's T5 violation); the good form opens the same territory without predetermining the answer. Build ✓, 453 tests ✓.
- Messaging audit — content redaction, metadata-only (2026-04-16): Hotfix on top of ADR-035. Pre-fix schema stored full message text in `messaging_events.content` — user-authored inbound, Jove-authored outbound, and **OTP codes on OTP-send rows**. Caught at Checkpoint E of the Sendblue migration before any real user text flowed through the system. Violated the CLAUDE.md Security Rule against logging user content and auth tokens. **Fix (ADR-037):** `src/lib/messaging/send.ts` now exports `MessageContentKind = "otp" | "user" | "jove" | "system"` and a `redactForAudit()` helper. Every outbound insert runs content through the helper before writing to `messaging_events`. Replacement strings: `[OTP_SEND]`, `[USER_MSG len=N]`, `[JOVE_REPLY len=N]`, `[SYSTEM_MSG len=N]`. Default when unspecified is `"system"` — safe fallback. Inbound Sendblue webhook applies the same transform inline (`[USER_MSG len=N]`) and replaces `raw_payload` with a fields-only projection so the full message body never lands there either. Sendblue outbound insert now omits `raw_payload` entirely because Sendblue's response echoes full content in it. **Call sites tagged (16 total):** all 14 in `src/lib/linq/message-router.ts` (keyword/media/rate-limit/fallback/unknown-number/confirm-ack/refine-ack/reject-ack → `"system"`; Jove response, checkpoint text, post-confirm, post-refine, post-reject follow-ups → `"jove"`), plus `src/app/api/user/phone/route.ts` OTP send → `"otp"` and `src/app/api/user/phone/verify/route.ts` greeting → `"system"`. **Backfill:** new migration `20260417000008_redact_messaging_events_content.sql` nulls `content` on any legacy rows whose value doesn't already start with `[`. Idempotent. Applied to prod. **Tests:** 442 pass — the route test mocks `sendMessage` by shape, so the new `contentKind` param passed through doesn't affect existing assertions. Build ✓, lint ✓. See ADR-037.
- Beta Health panel — PR 3 of 3: feedback panel (2026-04-16): Third and final panel for the Beta Health tab. **UI only — no new routes.** Reuses the existing `/api/admin/beta-feedback` endpoint (GET returns `{ items, unread_count }`, PATCH `{ id }` marks one read) already consumed by `BetaFeedbackTab`. **Panel** — new `src/components/admin/FeedbackPanel.tsx` mirrors `ApiErrorsPanel` layout conventions (mono uppercase section header, hairline-bordered list, "✓ Clean" collapse when empty). Narrower scope than `BetaFeedbackTab`: unread rows only, no pagination, no full history. Each row renders user_email, timestamp, optional page_context, the feedback_text in full, and a monospace "Mark read" button. When `unread_count === 0`, collapses to the same "✓ Clean — No unread feedback" banner ApiErrorsPanel uses. For full history browsing, admin still goes to the Feedback section. **Data flow** — `admin/page.tsx` calls `data.loadBetaFeedback()` when `section === "health"` so the panel loads with fresh data. `useAdminData` is the single source of truth — marking read in the Health panel updates `betaFeedbackUnreadCount` immediately and the Feedback-tab nav badge tracks it. No new badge on the Health nav entry for feedback (per scope — Health's red pill is for API errors; the Feedback tab's existing badge remains the primary nag for unread). Mounted below `ActiveUsersPanel` in the Health section. Verified end-to-end: submitted a test feedback via `BetaFeedbackButton` → appeared in panel with ⚠ 3 unread + all rows → clicked Mark read → row disappeared, count dropped to 2 → marked the remaining two → panel collapsed to ✓ Clean banner, Feedback nav badge hidden. Beta Health panel triptych now complete (errors, active users, feedback). Build ✓, 453 tests ✓.
- fix(api): force dynamic rendering on 17 routes using cookies (2026-04-16): Real issue surfaced by the Beta Health Errors panel within hours of PR 1 shipping — Next.js was logging "Dynamic server usage" errors during static-generation analysis on every route that used `@/lib/supabase/server createClient()` (or `verifyAdmin()`, which wraps it). Both paths call `next/headers cookies()` which is strictly dynamic. Routes worked at runtime but the build-time warnings were being caught by the outer try/catch wraps PR 1 added and flooding `api_errors` with ~10 noise rows per build. Fix is a single line per file: `export const dynamic = "force-dynamic";` right after imports. Tells Next.js not to attempt static rendering at all. 17 routes touched (admin: beta-allowlist, beta-feedback, conversations, feedback, manual, messages, users, waitlist; user: /api/user/phone, /api/user/phone/verify; public: /api/manual, /api/conversations, /api/analytics/identity, /api/onboarding-status, /api/auth/signup, /api/auth/logout, /api/settings/link-phone). Not touched: edge-runtime routes (already opt out of static rendering), admin routes that already had the export. Also issued SQL to purge stale rows: `delete from public.api_errors where error_message like 'Dynamic server usage%';`. Nice "tool works" moment — the observability shipped in PR 1 caught an issue within hours that would have otherwise sat as noise in the build logs. Build ✓, 453 tests ✓.
- Beta Health panel — PR 2 of 3: active beta users (2026-04-16): Second of three panels for the admin Health tab. Shows who's on the beta allowlist, who's signed in ever, and who was active in the last 24h / 7d — the "who should I check on" signal at 7am after beta launches. **Endpoint** — new `src/app/api/admin/active-users/route.ts` runs parallel queries: `beta_allowlist (email, created_at)` and `supabase.auth.admin.listUsers({ perPage: 1000 })` (matches `/api/admin/beta-feedback` — default 50/page would silently truncate). Builds an email → authUser map (lowercased both sides for safety), emits per-user rows (email, user_id, allowlisted_at, signed_in_ever, last_sign_in_at) plus a 4-stat summary (total_allowlisted, ever_signed_in, active_last_24h, active_last_7d). verifyAdmin() JWT guard. No new migration. **Panel** — new `src/components/admin/ActiveUsersPanel.tsx` mirrors `ApiErrorsPanel` layout conventions (mono uppercase section header, card treatment, hairline-bordered list). No window selector — the data is a single snapshot. No nav badge — the list of beta users is reference info, not something to nag about. Summary banner renders as a 4-column stat grid with mono uppercase labels and sans-500 values. User rows: email + optional `· INVITED, NO SIGN-IN` chip when `last_sign_in_at` is null + relative "allowlisted Nd ago" + relative "Nd ago / today / yesterday / never" last-sign-in. Exact ISO timestamps available on hover via `title` attr. Mounted below `ApiErrorsPanel` in the Health section of `admin/page.tsx`. Verified locally on prod allowlist: 5 allowlisted, 3 signed-in-ever, 1 active 24h/7d, "today" rendered correctly for recent activity. Build ✓, 453 tests ✓.
- Beta Health panel — PR 1 of 3: errors from last 24h (2026-04-16): First of three panels planned for the admin Health tab ahead of beta launch. Catches uncaught API errors that users don't report — 500s, thrown exceptions, and Supabase query failures on wrapped routes — so a 7am "what broke overnight" check is one glance at /admin?section=health. **Table** — new migration `20260417000007_api_errors.sql` creates `public.api_errors` with columns (route, method, status_code, error_message, error_stack, user_id_hash text (16-char hex from hashUserId, no FK), request_id, created_at), two indexes (created_at DESC, (route, created_at DESC)), RLS enabled with no user policies — service_role writes and reads via admin API only. Two SECURITY DEFINER RPCs mirror `admin_confirm_stats` / `admin_confirm_recent_failures`: `admin_api_error_stats(p_window_seconds)` returns grouped counts by route plus a total; `admin_api_error_recent(p_limit)` returns the last-N feed. Both granted only to service_role. Migration numbered 7 because the in-progress messaging branch claimed 5 and 6. **Logger** — new `src/lib/observability/record-api-error.ts` mirrors `recordConfirmFailure`: takes the admin client as a param, awaits the insert (small latency vs. losing observability rows under Edge Runtime unhandled-promise warnings) but never throws. Hashes user_id server-side via existing `hashUserId()` so the table never stores raw ids. Captures error_stack when error is an Error instance. **5 routes wrapped** — `/api/chat` (edge), `/api/conversations`, `/api/manual`, `/api/analytics/identity`, `/api/session/summary` (edge). Pattern: `let capturedUserId` declared before try; catch reports the user + route + method + error + stack. Internal 500 paths (conversations.insert failure in `/api/chat`, convError in `/api/conversations`, null summary in `/api/session/summary`) log explicitly so Supabase query failures are covered too. `/api/checkpoint/confirm` intentionally NOT wrapped — it has domain-specific `confirm_failures` logging already. Routes that intentionally return 4xx (auth, validation) also not wrapped — those aren't silent failures. **Admin endpoint** — new `src/app/api/admin/errors/route.ts` mirrors `/api/admin/confirm-stats`. `verifyAdmin()` JWT guard, parallel RPC calls, windowSeconds param clamped to [60, 7d], default 24h. **Panel UI** — new `src/components/admin/ApiErrorsPanel.tsx` mirrors `ConfirmHealthPanel` layout inside `/admin?section=health` below `SchemaHealthTab` + `ConfirmHealthPanel`. Window selector (1h/24h/7d), ✓ clean banner or ⚠ alert with by-route breakdown, recent feed (last 50) with `METHOD /route · HTTP STATUS` header and error message ellipsis. **Health nav badge** — `useAdminData` gains `apiErrorsCount` state + `loadApiErrorsSummary` action; `/admin/page.tsx` calls it on every section change (cheap RPC round-trip) so the Health tab's red-pill badge refreshes as the admin navigates. Same treatment as the feedback unread count. Verification deferred post-migration-apply: force a test throw in `/api/chat`, hit as signed-in user, confirm row in `api_errors` + badge + panel render, then remove. Build ✓, 453 tests ✓.
- Design 2.0 — Step 5 (final): wordmark tracking 4→1.5px + landing hero 34→42px (2026-04-16): Closes Design 2.0 with a smaller slice than originally scoped. **Wordmark tracking unified across 8 surfaces** (MobileSession, MobileManual, EntryScreen, LoginScreen, InfoScreens, SeedScreen, waitlist, reset-password): `letterSpacing` 4px → 1.5px. At 20px that drops from 20% to 7.5% of font size; at 13px from 30% to 11%. Both now inside editorial-appropriate tracking ranges. 4px was reading as decorative flourish; 1.5px reads as considered label. Canvas masthead at 52px with `-0.5px` tracking unchanged from Step 4 — large type and small type want different tracking, and the principle "one name, one treatment, different scales" is loose on tracking by design. **Landing hero** (`EntryScreen` "Map your operating system.") bumped 34px → 42px inside the phone at all viewport widths. Approaches Instrument Serif's display-character threshold (~50px) while staying within the 430px phone width (wraps cleanly to "Map your operating / system."). **Canvas right-margin display hero (Option B) considered and rejected**, prototyped twice: (1) hero + subhead + whisper on canvas / phone shows only CTAs — phone went hollow with ~500px of empty linen between wordmark and CTAs, read as frame-around-buttons rather than product preview; (2) hero + subhead on canvas / whisper stays in phone — composition's cohesion fluctuated with whisper length (rotating 10 sentences, half single-line reads as stranded, half two-line holds together), oscillating pass/fail every 4.5s. Honest gate call per explicit instruction: fall back to Option A without further iteration. Canvas right margin on landing remains empty, matching authenticated's intentional negative space. The authenticated-vs-unauthenticated-desktop asymmetry principle remains available for a future pass but was NOT documented as an ADR — there's no asymmetry to document yet; the composition's phone-as-product-preview side would need something denser than CTAs alone (a mock chat surface, or a more substantial editorial artifact) before Option B could pass. Build ✓, 453 tests ✓.
- Design 2.0 — Step 4 complete: shared DesktopVitrine, Jove rail, paper surface, 17px Jove body, side-margin layout (2026-04-16): Four stacked commits closing Design 2.0 Step 4. **DesktopVitrine extraction + paratext consistency fix** — new `src/components/layout/DesktopVitrine.tsx` owns the parchment canvas + masthead + phone frame + colophon scaffolding; both `MobileLayout` (authenticated) and `OnboardingFlow` (landing) wrap their children in it, so landing and authenticated surfaces share one framing by construction (structurally impossible to drift). Masthead dropped 64px → 52px with tight -0.5px tracking (was pulling eye before the phone at 64). Colophon left-aligned in a column whose left edge aligns with the phone (centered 320px wrapping was reading "wedding invitation"; left-aligned matches editorial colophon convention — NYRB, NYR, Ghost). 120px centered hairline between statement and meta removed (orphaned ornament under left-aligned text; typeface change Instrument Serif → JetBrains Mono is itself the register rule). **Jove rail treatment** — `MobileSession.tsx` persona message body and typing indicator drop the tinted-bubble + borderRadius in favor of a 2px `--session-persona-soft` left rail with 14px text indent and 4px vertical padding. Typing and streaming share 4px padding (an annotation mark doesn't resize to fit what it's annotating). JOVE label paddingLeft 4 → 0 so label aligns with rail's left edge, marginBottom 4 → 6px so label sits tethered to rail without touching it. Welcome panel is structurally different (nothing to annotate yet; chips are utilitarian UI) and correctly drops the rail while keeping the JOVE label as voice-introduction — rail is annotation grammar, welcome is introduction grammar. Checkpoint cards unchanged (moment, not utterance — they retain their heavier gradient + shadow + radius treatment). Prototyped and verified at one-sentence, medium, twenty-sentence, and welcome lengths. **Paper surface** — tab panel `backgroundImage` composites the existing 2.5% noise SVG with a new `radial-gradient(ellipse at center, transparent 50%, rgba(26,22,20,0.04) 100%)` corner vignette (4% ink-darken at the far corners, threshold-of-perception). Same vignette applied to `OnboardingFlow`'s inner wrapper so landing and chat share one paper feel. `ChatInput` flat 1px `borderBottom` replaced with a paper-groove composite: `inset 0 1px 0 rgba(255,255,255,0.5)` constant top highlight + `inset 0 -1px 0 <state-color>` bottom shadow (`--session-ink-hairline` idle, `--session-persona-border` focus, `--session-persona-soft` recording). Input reads as a recessed zone pressed into the linen rather than a line drawn on top. The global focus ring on `<textarea>` coexists without fighting because it's on a different element and a different visual register (inset groove = ambient "this is the input zone"; focus ring = active "your keyboard is here"). **Typography** — Jove body 16px → 17px (matches `--size-prose`) in chat messages, welcome prose, and the returning-user "What's on your mind?" prompt. User messages stay at 15.5px, preserving the 1.5px differential between drafted input (17) and committed history (15.5). Checkpoint card body stays at 16px Instrument Serif — its hierarchy is managed separately. **Side-margin desktop layout** — at viewports ≥1030px, masthead relocates from top-center to top-LEFT corner (x=48, y=48) and colophon from bottom-center to bottom-LEFT (x=48, y=bottom-48, column ≈ 220-320px adaptive via `min(320px, calc((100vw - 430px) / 2 - 80px))`). Phone drops the 400px vertical reservation and takes full natural `min(932px, 100dvh)` maxHeight — paratext exits the vertical sightline, phone sits at full height, the horizontally-empty left margin does paratext work, and the right margin stays as intentional negative space. Below 1030px, fallback to the prior vertical stack with centered masthead + colophon + 400px phone reservation (existing narrow-desktop users unaffected). Responsive behavior lives in new `globals.css` classes (`.mw-masthead`, `.mw-colophon`, `.mw-colophon-inner`, `.mw-phone-frame`) with media queries at 431px (mobile/desktop) and 1030px (narrow/wide desktop). Verified at 375×812 (mobile unchanged), 1029×900 (fallback), 1030×900 (threshold transition), 1280×900, 1440×900. Build ✓, 453 tests ✓.
- Messaging migration — Sendblue for 1:1, Linq for groups (2026-04-16): Replaces Linq for 1:1 SMS/iMessage. Dual-provider permanently: 1:1 text flows through Sendblue at `+16292925296`, group facilitator stays on Linq because Sendblue does not expose `participant.added` / `participant.removed` / `chat.created` events or a `getChatInfo` equivalent. **Architecture:** new `src/lib/messaging/` module contains `provider.ts` (env flag), `phone.ts` (strict E.164 validation), `sendblue.ts` (API client — `sb-api-key-id` + `sb-api-secret-key` headers, POST `/api/send-message`, throws on non-2xx with parsed `error_message`), `linq.ts` (wrapper that adapts Linq's chat_id API to the unified shape, encapsulates phone → `phone_numbers.linq_chat_id` lookup and first-message `createChat` fallback with automatic chat-id persistence — non-throwing, returns `{ok: false, ...}` on failure), and `send.ts` (unified `sendMessage({to, content, ownerUserId?, linqGroupChatId?})` — non-throwing contract preserved so existing callers that checked `{ok: false}` keep working). Routing: `linqGroupChatId` present forces Linq; otherwise `MESSAGING_PROVIDER` (defaults to `sendblue`). **Audit:** new `messaging_events` table (20260417000005) writes one row per outbound send and one per inbound webhook. Partial unique index on `(provider, provider_message_id) where provider_message_id is not null` dedupes Sendblue retries (3× with 45s timeout). RLS enabled, no public policies — service-role only, same pattern as `safety_events`. **Inbound:** new `/api/webhooks/sendblue/route.ts` logs the header key set on first deploy so the secret header name can be confirmed, then returns 200 on every non-5xx path (idempotent insert catches 23505 and treats as expected). Signature verification deferred until first real webhook lands. **Call sites migrated (1:1 only):** `src/lib/linq/message-router.ts` (keyword responses, unknown-number, rate-limit, media-only, Jove response, checkpoint follow-ups, fallback — `handleCheckpointResponse` gains a `senderPhone` param; `sendTypingIndicator` + `markAsRead` stay Linq-direct since Sendblue does not expose them), `src/app/api/user/phone/route.ts` (OTP send), `src/app/api/user/phone/verify/route.ts` (greeting send, manual `linq_chat_id` store-back removed — wrapper handles it). **Untouched (groups stay on Linq):** `src/lib/linq/group-bridge.ts`, `src/lib/linq/group-detection.ts`, `src/lib/linq/group-state.ts`, `src/app/api/linq/webhook/route.ts` (now carries a deprecation banner for the 1:1 path only). **E.164 enforcement:** migration 20260417000006 adds CHECK constraint `phone_numbers_phone_e164` (idempotent via pg_constraint guard); phone audit on prod showed 2/2 rows already strict E.164 so the drafted normalize script was unnecessary and deleted. **Env:** `MESSAGING_PROVIDER=sendblue`, `SENDBLUE_API_KEY_ID`, `SENDBLUE_API_SECRET_KEY`, `SENDBLUE_FROM_NUMBER=+16292925296`, `SENDBLUE_WEBHOOK_SECRET`, `NEXT_PUBLIC_MESSAGING_FROM_NUMBER=+16292925296` added across Prod/Preview/Dev. `LINQ_*` and `NEXT_PUBLIC_LINQ_PHONE_NUMBER` retained for rollback. **Rollback:** flip `MESSAGING_PROVIDER=linq` (outbound only); inbound stays dual-wired. See ADR-035 (dual-provider architecture) and ADR-036 (no dev Supabase — migration applied to prod via dashboard paste; infrastructure debt to resolve before scale). Side fix: `.env.local` had `DEEPGRAM_API_KEY` and `DEV_USER_EMAIL` concatenated onto one line — split. Build ✓, 442 tests ✓.
- PostHog follow-ups — PII guard test, init hardening, startExploration 401 leak fix (2026-04-16): Three small follow-ups on top of PR 2, each shipped straight to main. **Regression guard** — new `src/lib/analytics/events.test.ts` (11 cases, one per tracker) mocks `posthog.capture`, calls each `trackX` with a valid payload, and asserts (a) correct event name, (b) no forbidden key from `{content, message, text, body, email, phone, user_id, display_name, reason, excerpt}`, (c) expected keys present. Strict equality on forbidden keys so existing `message_number` / `message_count` pass; if someone later adds a `content` or `excerpt` field to an event payload, CI fails before it reaches PostHog. Test count 442 → 453. **Init hardening** — `initPostHog()` now wraps `posthog.init()` in try/catch (if the library ever throws synchronously we warn once and leave `initialized=false`; events and pageviews silently don't fire, which is the correct failure mode for analytics) and adds `advanced_disable_decide: true` to skip the `/decide` endpoint, which powers feature flags we don't use and was otherwise logging a blocked-request error when `*.posthog.com` was blocked by an ad-blocker, network rule, or extension. Config-fetch failures on `/array/{key}/config.js` were already handled internally by posthog-js (captures batch to localStorage and retry). **startExploration 401 leak** — latent state leak at `src/lib/hooks/useChat.ts:830` in the exploration flow's 401 branch: `router.push('/login')` was called without first clearing `isLoading`. Moot in practice (redirect unmounts the component) but the function-entry guard `if (isLoading || isStreaming) return false` would block re-entry if the leak ever mattered. Discovered during an audit of a "send button stuck disabled" report that Jeff flagged during mywalnut.app testing; the audit didn't find code evidence for the main symptom but surfaced this one real latent bug. One-line fix: `setIsLoading(false)` before the redirect. Build ✓, 453 tests ✓.
- PostHog analytics — Stage 1 events + permanent recording-off (2026-04-16, PR 2 of 2): Wires the event taxonomy on top of PR 1's plumbing and resolves the "replay recording" question with a permanent off. Mywalnut is a deep personal-disclosure product; recording user screens crosses a trust line that aggregate events never do. **Client config:** `disable_session_recording: true` is permanent (no longer the temporary fixup from PR 1), and the `session_recording` masking block was removed since it's unreachable with recording off. Belt-and-suspenders: project-level recording also disabled in PostHog Settings → Recordings so an accidental client config change still captures nothing. **Event taxonomy:** new `src/lib/analytics/events.ts` exports typed tracker functions for the Stage 1 set — `conversation_started`, `message_sent`, `conversation_ended`, `checkpoint_proposed/confirmed/rejected/refined`, `manual_viewed`, `manual_exported`, `entry_edited`, `session_started`. Every tracker's prop shape is tight: no content, content_length, rejection reason text, or other user-authored fields. `EntryPoint` union starts at `"situation"` (resonant-content + personal-upload literals added when those flows ship). **Instrumentation:** `src/lib/hooks/useChat.ts` fires conversation_started when `conversationId` transitions null→value in finalizeMessage (and again in startExploration), message_sent for user+assistant after successful persistence (ordered chronologically, message_number snapshotted from messages.length at send time), checkpoint_proposed when setActiveCheckpoint runs (captures proposedAt ref), and checkpoint_confirmed/rejected/refined on button action carrying time_to_decision_ms. conversation_ended fires in startNewSession with end_type="natural" and duration_seconds from a conversationStartedAt ref. `src/components/MainApp.tsx` fires manual_viewed in a tab-watching useEffect with days_since_last_view derived from `mw_last_manual_view` localStorage (key updated after capture). `src/components/mobile/MobileManual.tsx`'s doExportAndShare fires manual_exported (format:"pdf", entry_count) after the PDF generates + shares successfully. `src/components/PostHogProvider.tsx` fires session_started after identify resolves, using `mw_last_session` localStorage for days_since_last_session + is_first_session; a ref guard blocks React Strict Mode double-invoke in dev. **Privacy policy:** new "Analytics" section in `src/app/privacy/page.tsx` naming PostHog, hashed user IDs, the explicit "we do not record your screen" line, and the Do Not Track opt-out; last-updated stamp bumped to 2026-04-16. `entry_edited` tracker exists but isn't called anywhere — there is no entry-edit UI yet. Verified locally: distinct_id stays the 16-char hex hash across session, session_started fires once (ref-guarded), manual_viewed fires on tab click, no `/s/` snapshots and no `posthog-recorder.js` load. Build ✓, 442 tests ✓.
- PostHog analytics foundation — plumbing only (2026-04-16, PR 1 of 2): PostHog browser SDK wired for the beta. **PR 1 scope is plumbing, not events** — identifies users, captures pageviews, no Stage 1 events yet. `posthog-js` + `posthog-node` installed. `src/lib/analytics/posthog-client.ts` inits with `autocapture: false` (so user-authored text from messages / manual entries cannot leak into event properties), `capture_pageview: false` (we capture manually with pathname only — no query strings, which could carry PII), `respect_dnt: true`, `persistence: "localStorage+cookie"`, and `disable_session_recording: true` as a temporary flag until PR 2 wires `data-ph-mask` on the chat + manual surfaces. `src/app/api/analytics/identity/route.ts` returns the current user's hashed id via the existing `hashUserId()` helper in `src/lib/observability/log.ts` and the `OBSERVABILITY_USER_ID_SALT` env var — reusing Track 4's hash keeps PostHog distinct_ids correlatable with `confirm_failures` rows without sending raw user_ids to a third party. Hash is 16 hex chars (~64 bits entropy), not 64. `src/components/PostHogProvider.tsx` inits once, calls `identify(hashedId)` on mount, and captures `$pageview` events with the pathname on every route change. The pageview tracker is wrapped in a Suspense boundary (via a nested `PageViewTracker` component) so `useSearchParams` at the root layout does not opt the whole app out of static rendering. Wired into `src/app/layout.tsx` — provider wraps `{children}` in the body. Two env vars added to Vercel: `NEXT_PUBLIC_POSTHOG_KEY`, `NEXT_PUBLIC_POSTHOG_HOST`. `OBSERVABILITY_USER_ID_SALT` remains unset in prod as of ship — the helper falls back to the shared dev salt `mywalnut-dev-salt-v1`; set a distinct prod salt to prevent prod/dev hash collisions. Verified locally in preview that PostHog init + identify + pageview capture all return 200, and after the `disable_session_recording` fixup the recorder script stops loading and no `/s/` snapshots post. Build ✓, 442 tests ✓.
- Design 2.0 — desktop paratext (masthead + colophon) (2026-04-16, step 3/4): Path A for the desktop canvas — commits the empty parchment field around the phone frame to an editorial composition. The phone is the reading surface, the canvas around it is paratext. `MobileLayout.tsx` gains `<header>` and `<footer>` siblings on the outer parchment canvas. Masthead: `my walnut` at 64px Instrument Serif, letter-spacing `-0.5px`, weight 400, color `--session-ink-soft`, absolute-positioned `top: 48px`. Same name and typographic treatment as the in-phone 20px wordmark, rendered larger — one name, one treatment, different scales. Lowercase held at scale with tight negative tracking so it reads intentional, not default; color `--ink-soft` keeps it a label, not a shout. Colophon: two Instrument Serif 18px statement sentences in `--ink-soft` ("A private manual, written by you and assembled in conversation. / Nothing enters it unless you confirm."), a 120px centered hairline, then a JetBrains Mono 12px mixed-case `--ink-mid` meta row ("In closed beta. v{APP_VERSION} · Privacy · Terms") using `var(--size-meta)` and flex-gap for separator spacing. Mixed-case is deliberate: app structural labels (nav tabs, JOVE label, Settings section headers) use uppercase mono as UI register; the colophon is editorial register, closer to a book colophon line. No hairlines around the phone frame — whitespace defines zones, not rules; the one rule inside the colophon marks where editorial statement meets structural metadata. Phone's `maxHeight` on the canvas recalibrated from a flat `932px` to `min(932px, 100dvh - clamp(0px, (100vw - 430px) * 999, 400px))` — reserves 400px of vertical space for paratext at desktop widths while leaving mobile's full-viewport behavior untouched (clamp resolves to 0 at ≤430px). Two new CSS classes in `globals.css`: `.mw-desktop-paratext` (display: none on mobile, block at ≥431px) and its inverse `.mw-mobile-only` (hides at ≥431px, used on the EntryScreen's in-phone Privacy/Terms footer so the canvas colophon's legal links aren't duplicated and the phone doesn't read as a self-contained document competing with the publication it's inside). `APP_VERSION` imported from `src/lib/version.ts` for the meta row. The "In closed beta." string has an EDITORIAL COPY comment directly above it flagging it as intentional publication metadata (e.g. future "First edition, 2026") rather than a forgotten flag — deliberate string edit at beta exit, not an automated transition. Verified at 1280×900, 1440×900, and 375×812 viewports. On mobile the paratext hides cleanly and the phone fills the viewport exactly as before. Build ✓, 442 tests ✓.
- Observability + integration tests — closes hardening plan (2026-04-17, Tracks 4 & 5): Two tracks shipped together to close the checkpoint hardening plan. **Track 4 (observability):** new migration `20260417000004_confirm_failures.sql` creates `confirm_failures` table (user_id, message_id, conversation_id, error_kind, error_detail, status_code, duration_ms, created_at) with RLS enabled and three indexes (created_at DESC, user_id, error_kind+created_at). Two SECURITY DEFINER RPCs: `admin_confirm_stats(p_window_seconds)` returns failure counts grouped by kind within the window; `admin_confirm_recent_failures(p_limit)` returns recent feed. New `src/lib/observability/log.ts` with `logEvent()` JSON line emitter (dev mode pretty-prints, prod is single-line JSON for Vercel logs) and `hashUserId()` using Web Crypto SHA-256 with fixed salt from `OBSERVABILITY_USER_ID_SALT` env var (falls back to dev salt). Fire-and-forget `recordConfirmFailure()` in `src/lib/observability/record-failure.ts` awaits the DB write but never throws. `/api/checkpoint/confirm/route.ts` fully instrumented: `confirm_attempt` at start, `confirm_rpc_ok`/`confirm_rpc_fail`, `confirm_stream_started`, `confirm_outcome` on every terminal path (success, idempotent, 4xx, 5xx, rate-limit, unauthorized). Every 4xx/5xx also writes a `confirm_failures` row. New `/api/admin/confirm-stats` API (nodejs runtime, admin-gated) and `ConfirmHealthPanel.tsx` component rendered below `SchemaHealthTab` in `/admin?section=health`: window selector (1h/24h/7d), ✓/⚠ banner with failure count + kind breakdown, last-20-failures feed. 5 new `hashUserId` tests (determinism, output shape, no-leak). **Track 5 (integration tests):** new `vitest.e2e.config.ts` (separate config, fileParallelism:false, 30s test timeout, excluded from unit suite) and `npm run test:e2e` script. `src/db-contract.e2e.test.ts` is the drift canary — attempts real inserts into every table the code writes to (manual_entries full + minimal shapes, messages assistant + system, conversations update, profiles persona_mode, safety_events crisis path, confirm_failures, beta_feedback). Would have caught yesterday's `type` column drift. `src/lib/persona/confirm-checkpoint.e2e.test.ts` exercises the actual `confirm_checkpoint_write` RPC — happy path, idempotent repeat, concurrent invocations (exactly one row), checkpoint_not_found, checkpoint_not_pending. New `.github/workflows/supabase-e2e.yml` spins up local Supabase (Docker on Ubuntu runner) via `supabase start`, applies migrations, extracts creds, runs `npm run test:e2e` on every PR and main push. Build ✓, 442 unit tests ✓ (was 437, +5). E2E suite runs in CI only — local E2E requires Docker Desktop which the owner hasn't installed.
- Client resilience for checkpoint confirm (2026-04-17, Track 3 of hardening plan): Checkpoint confirm now auto-retries transient failures and surfaces specific, actionable error messages instead of the generic "Something went wrong." `useChat.ts:confirmCheckpoint` wraps the fetch in a retry loop: max 3 attempts, backoffs 500ms / 2s between attempts, 30s per-attempt timeout via `AbortController`. Retries on network rejection or 5xx. Does NOT retry on 4xx (404 gone, 400 rejected-already, 403 forbidden) or 429 (rate-limited) — those are surfaced with specific messages. 401 short-circuits to `/login`. Server-side idempotency from Track 2 means retries never produce duplicates. Mid-stream failures AFTER a successful write no longer surface as errors — the atomic RPC guarantees the DB is consistent, so `streamFromResponse` exceptions just log and proceed to `loadManual()`. New `confirmErrorMessage(status, networkFailed)` helper exported from `useChat.ts` covers the full taxonomy (network, 400, 404, 429, 5xx, generic fallback). 22 new tests in `src/lib/hooks/confirm-error-message.test.ts` including a message-quality pass that checks every status returns a non-empty sanitized sentence (no "undefined" leakage, ends with punctuation). SW audit (`docs/reference/sw-audit.md`) confirmed `/api/*` is network-only in `public/sw.js:46`, no SW intervention possible on API calls — yesterday's `(failed) net::ERR_` row on `sw.js:69` was the navigation offline fallback, unrelated to checkpoint confirm. Build ✓, 437 tests ✓.
- Idempotent + transactional checkpoint confirm (2026-04-17, Track 2 of hardening plan): `/api/checkpoint/confirm` is now safe to call any number of times with the same messageId — no duplicate writes, no "already resolved" 500s. New migration `20260417000003_confirm_idempotency.sql` adds: (a) partial unique index `manual_entries_user_source_msg_uniq` on `(user_id, source_message_id) WHERE source_message_id IS NOT NULL` — prevents duplicate rows for the same checkpoint, doesn't constrain populate-shaped null-source rows; (b) `confirm_checkpoint_write()` SECURITY DEFINER function that runs the three writes (insert manual_entries, update message status, insert system message) atomically with `FOR UPDATE` locking on the message row. Returns `(entry_id, was_already_confirmed)`. Raises `P0002 checkpoint_not_found` or `P0001 checkpoint_not_pending` for specific error states. `confirm-checkpoint.ts` rewritten to call the RPC — TS still handles composed-field fallbacks (composed_content → message.content stripped of crisis resources, composed_name → meta.name → "Untitled", derived summary from first sentence) but delegates the write. Route handler maps RPC errors to precise HTTP statuses (404 for not-found, 400 for rejected/refined, 500 for other), and returns a short JSON ack (not SSE stream) when `wasAlreadyConfirmed=true` so the follow-up doesn't duplicate. Client `useChat.ts:confirmCheckpoint` detects content-type and skips stream parsing on JSON ack. 10 new tests (5 in confirm-checkpoint.test.ts covering composed field handling + RPC error mapping + idempotent return, 5 in route.test.ts covering error-to-status mapping + JSON-vs-SSE response shape). Build ✓, 415 tests ✓.
- Schema Health admin panel + CI for migrations (2026-04-17, closes Track 1): New `/admin?section=health` panel surfaces migration state — applied in prod (queried via new `admin_list_migrations()` SECURITY DEFINER function in migration `20260417000002_admin_list_migrations.sql` that wraps `supabase_migrations.schema_migrations`), files on disk (via `fs.readdir` on `supabase/migrations/`), and the diff between them. Banner reports "✓ In sync" or "⚠ Drift detected" with specific missing-file / missing-applied lists. Table view shows every version with ✓/— for on-disk and in-prod columns. New `/api/admin/migration-status` route (nodejs runtime, admin-gated, dynamic). `SchemaHealthTab.tsx` component. Also shipped `.github/workflows/supabase-migrations.yml`: on PR touching `supabase/migrations/**` it prints the migration diff in the check summary; on push to main it runs `supabase db push` to apply. Concurrency group serializes rapid merges. Two repo secrets required: `SUPABASE_ACCESS_TOKEN` and `SUPABASE_DB_PASSWORD` (both set). This makes today's first CLI-driven migration (`20260417000002`) the reference for the flow going forward.
- Migration tooling + baseline squash (2026-04-17, Track 1 of hardening plan): Supabase CLI linked to prod project (`nkmperzwcmttdkxwhbiv`). Pre-flight audit ran on prod: RLS confirmed on all 13 public tables, every code-expected column present, zero orphan `manual_entries`. Generated `20260417000000_squash_baseline.sql` via `pg_dump` against prod — 1165 lines capturing all tables, indexes, functions, policies, grants, triggers. Deleted 12 pre-squash migration files and 5 loose SQL files (`supabase/schema.sql`, `add-extraction-snapshot.sql`, `add-feedback-table.sql`, `add-group-conversation-link.sql`, `admin-migration.sql`). Marked squash as applied in prod via `supabase migration repair`. Follow-up migration `20260417000001_fix_cleanup_function.sql` fixes a drift found in the squash — `cleanup_stale_anonymous_users()` was still referencing the pre-rename `manual_components` table; function body now points to `manual_entries`. Removed the defensive `extraction_snapshot` fallback in `call-persona.ts:343` (column is guaranteed present). Updated `docs/system.md` Migration Protocols section to mandate CLI-driven migrations (dashboard SQL editor now read-only). `scripts/test-phase1-gate.sh` reference to deleted `schema.sql` updated. Remaining Track 1 follow-ups: CI `supabase db push` on merge, admin Schema Health panel. Build ✓, 405 tests ✓.
- Design 2.0 — Tailwind token projection + mobile nav/input cramping fix (2026-04-16): Two-step Design 2.0 slice shipped together. **Step 1 (tokens):** projects existing `--session-*` CSS variables from `globals.css` into `tailwind.config.ts` under `theme.extend.colors`, `fontFamily`, `fontSize`. Utility classes like `bg-linen`, `text-ink-soft`, `font-persona` now available. No new tokens introduced — every value points at an existing var. `font-sans`/`font-serif`/`font-mono` override Tailwind defaults to resolve to DM Sans / Instrument Serif / JetBrains Mono; `font-persona` exposes Source Serif 4 (Jove's voice). `text-ink` resolves to `--session-ink` (darkest) because grep confirmed it's the body default across ~57 `color:` uses versus ≤4 for softer variants. No components migrated in this pass — utility surface is available for new work and opportunistic edits; ADR-009 (inline styles) policy unchanged. Spacing, borderRadius, boxShadow, and animation deliberately NOT projected — each requires a design decision due in later Design 2.0 steps. Two naming quirks accepted as tech debt (`border-persona-border`, `text-error-text`) — logged for a later token-hygiene pass. **Step 2 (cramping fix):** recovers ~14–18px of chat feed height on mobile without architectural change. `MobileNav.tsx`: face `--font-serif` → `--font-mono`, size 12px → 11px, tracking 1.5px → 1.8px, padding 14/20 → 10/14 above safe-area, new `border-top: 1px solid var(--session-ink-hairline)` as a structural floor. Active tab color `--session-ink-soft` → `--session-ink` (darkest); inactive stays `--ink-mid` for accessibility — avoided `--ink-ghost` because 4:1 at 11px fails for dyslexic users in low-light conditions. `ChatInput.tsx`: outer padding `8 20 4` → `12 20 8` so input reads as a dedicated writing surface on linen, not utility chrome. Three-tab `main` paddingBottom reservation 68px → 52px across Session/Manual/Settings for consistent frame. Three tabs, absolute positioning, and phone-frame all retained — Manual is the product's output, not incidental navigation, so the decision to demote mid-beta was rejected. Known sub-44px tap-target debt on MobileNav scheduled for a dedicated accessibility pass (ADR-033). See decisions.md ADR-032, ADR-033, ADR-034. Build ✓, 405 tests ✓.
- Dev test tools — stop auto-confirming, stop wiping real data (2026-04-16): Two related footguns fixed. `dev-simulate` used to auto-call `confirmCheckpoint()` inside its loop, flipping the checkpoint's `status` server-side to `"confirmed"` before the user ever touched the UI — so manual clicks on the same card then hit the `status !== "pending"` guard in `confirm-checkpoint.ts` and returned 500 "Checkpoint already resolved." Now dev-simulate generates a single checkpoint and exits with it left in `"pending"`, so the real `/api/checkpoint/confirm` path drives the rest (and can actually be tested). Separately, both `dev-simulate` and `dev-populate` wiped all of a user's `manual_entries` (and dev-simulate also wiped `manual_changelog`, all conversations, and all messages) before running, destroying real test data on every click. dev-simulate no longer wipes anything — it creates a fresh conversation alongside existing data; use `/api/dev-reset` for clean-slate. dev-populate now narrows its delete to populate-shaped rows only (`name IS NULL`), preserving real confirm-generated entries which always have a composed name. `checkpointTarget` param still accepted by dev-simulate for backwards compat but ignored (loop exits at first checkpoint regardless). Removed now-unused `confirmCheckpoint` + `parseCheckpointIntent` imports. Build ✓, 405 tests ✓.
- Checkpoint confirm bug fix — DB schema sync (2026-04-16): User-visible fix for "Something went wrong saving that. Try again." on every checkpoint confirm. Root cause was three separate drift points between `supabase/migrations/` and the production DB — none of which had been run: (a) the `sage_*` → `persona_*` rename (`20260414_rename_sage_to_persona.sql`) — code selected `persona_mode` but DB still had `sage_mode`; (b) the `safety_events` table itself was never created from the baseline schema, so crisis writes had been silently failing against a ghost table; (c) the `type` column on `manual_entries` was never dropped (`20260407_drop_pattern_type.sql` targeted the old `manual_components` name and was orphaned by the later table rename), so every `confirm-checkpoint.ts` insert hit a NOT NULL constraint on a column the code no longer provides. Ran the rename in the dashboard. Two new idempotent retry migrations: `20260416_create_safety_events.sql` creates the missing table with the already-renamed column (`persona_included_988`), FKs to `conversations` + `profiles`, RLS enabled without policies (service-role writes only), helper indexes on `user_id` and `created_at`; `20260416_drop_manual_entries_type.sql` retries the column drop against the current `manual_entries` name and also cleans `manual_changelog.type`. Verified end-to-end: devtest user confirms a checkpoint → row with non-null `name` + `source_message_id` + Jove-composed content lands in DB → Manual tab renders it after the follow-up stream completes. Follow-up noted: `dev-populate/route.ts` wipes ALL of a user's manual_entries before reseeding (line 64), which masked the confirm bug during testing and is a footgun if ever run against a non-devtest admin — track for later narrowing to populate-shaped rows only.
- A11y + design hardening (2026-04-15): BetaFeedbackButton redesigned — icon replaced with "feedback" pill label; popover gains heading, subtitle, 6-row textarea, dedicated error/retry state, 2s auto-close on success. Persona token darkened (`--session-persona` #6B7A4F → #556B48, 5.53:1 on cream, passes WCAG AA); primary CTA buttons remain sage green. Global `:focus-visible` added to `globals.css` — cream+sage box-shadow ring on all focusable elements, beats inline `outline: none` via `!important`. `@media (prefers-reduced-motion: reduce)` guard neutralises all transitions/animations. Full ARIA pass across 19 files: `MobileNav` is `role="tablist"` with `aria-selected`/`aria-controls`/roving `tabIndex`; `MobileLayout` panels are `role="tabpanel"` with `aria-labelledby`; chat scroll container is `role="log"` `aria-live="polite"`; 7 modals/drawers gain `role="dialog"` `aria-modal`; icon-only buttons (hamburger, drawer close, send/mic) get `aria-label`; `SectionHeader`/`EntryItem` toggles get `aria-expanded`; `autoComplete`+`inputMode` on all email/password/phone/OTP inputs; `<main>` landmarks and `<h1>` headings on all onboarding + mobile screens. Body-copy contrast: 11 `ink-ghost` usages migrated to `ink-mid` across session, drawer, manual, settings, admin for WCAG AA. Design audit docs (`design-audit.md`, `design-audit-v2.md`) added at repo root for reference — not user-facing.
- Checkpoint experience hardening — Prompt C (2026-04-15): Three progress signals Jove delivers through the arc — EARLY FRAME (turns 3-4, new users only), DEPTH BUILDING (turns 5-7), CHECKPOINT APPROACHING (gate close). Each is tracked via a latching flag on `ExtractionState` (`early_frame_delivered`, `depth_signal_delivered`, `approaching_signal_delivered`); the extractor both triggers signals in `sage_brief` and detects delivery from the assistant's last message. Approaching signal is the only one that resets on thread shift. First-checkpoint educational wrapper moved out of the checkpoint and onto the approaching signal 1-2 turns *before* — so the user is primed before they see the reflection. Transition copy: "Something's taken shape from what you've told me" is gone; the only line is "I want to put something in your Manual." POST-CHECKPOINT rewritten as three explicit steps: confirm + name the layer structure, name the open thread, plant the return hook. Composition (`confirm-checkpoint.ts`) tightened: 80-150 word cap (was 80-300), NOT A RECAP rule, EXPLICIT BIND required, second exemplar. `validateComposedEntry` upper bound 300 → 150 to match. Tier 2 section renamed `PROGRESS SIGNALS` → `PACING` so the new Tier 3 block can own the name.
- Three-tier Jove prompt + no post-checkpoint fork + observation miss tracking (2026-04-15): Restructured `system-prompt.ts` into Tier 1 (constitutional) / Tier 2 (voice and behavior) / Tier 3 (conversation mechanics). `voice-autistic.ts` is the sole source for voice rules, banned phrases, and examples. Removed the post-checkpoint "Work with it / Keep building" fork — Jove now acknowledges briefly and returns to the conversation from whatever the user surfaces next; no scripted menu. Added `observation_miss_count` to `ExtractionState`: extractor increments on misses, resets on confirmations; the brief injects a grounding nudge at ≥2 and a full reset at ≥3. Rewrote `system-prompt.test.ts` for the tier structure and added miss-count injection tests in `extraction.test.ts`.
- Manual context compression + extraction window 6→12 (2026-04-15): New module `src/lib/persona/manual-context.ts` exposes `compressManualEntry()` + `prepareManualContext()`. Recent entries (current conversation + backfill up to 4 total) render in full; older entries render as one line: `[Layer N — LayerName] "Headline" — summary. Key words: w1, w2, w3.` — keeps the context budget bounded as returning users accumulate entries. Group-chat prompt path opts out (`currentConversationId=null`). Added `summary` (text) and `key_words` (text[]) columns to `manual_entries` via migration `20260415_add_manual_entry_compression_fields.sql`. `confirm-checkpoint.ts` composition prompt emits both fields in its JSON response; runtime fallback derives summary from the first sentence of content when absent (backfill-free). `loadConversationContext` now joins `source_message_id` → `conversation_id` so current-session entries can be distinguished. Extraction window: `EXTRACTION_MESSAGE_WINDOW = 12` constant; extractor slice widened from 6 to 12 messages so mechanisms developing across several turns get captured.
- Manual terminology unification (2026-04-15): Cosmetic follow-up to the `manual_entries` table rename. Brings code, prompts, docs, and admin UI in line with the canonical vocabulary (Manual / Layer / Entry / Checkpoint). TS type `ManualComponent` → `ManualEntry` across 11 files. UI `Thread`/`threads` → `Entry`/`entries` in `layer-definitions.ts`, `PopulatedLayer.tsx`, `MobileManual.tsx`, `AdminManualView.tsx`, `generate-manual-pdf.ts`. State and prop renames: `confirmedComponents` → `confirmedEntries` (`useChat`/`MainApp`/`MobileSession`), `components` prop → `entries` (`MobileManual`, `AdminManualView`, callers in `MainApp` and `UserProfilePane`). File rename: `PatternItem.tsx` → `EntryItem.tsx` (export + interface `ThreadCardProps` → `EntryCardProps`). Prompt strings: "confirmed components" → "confirmed entries" in `system-prompt.ts`; "behavioral model(s)" → "Manual(s)" in `classifier.ts`, `confirm-checkpoint.ts`, `extraction.ts`, `simulate-user.ts` (kept the legal AI-disclosure phrase in `rules.md` intact). Doc fixes in `system.md`. Admin user-list display: "manual component(s)" → "manual entr(y/ies)". DB column names and API response field names (e.g. `component_count`) intentionally left alone — public contract.
- `/feedback` slash command removed (2026-04-15): The in-chat slash command (intercepted in `useChat.ts` before reaching Jove) is gone. Users now submit feedback exclusively through the persistent `BetaFeedbackButton` (writes to `beta_feedback`). Removed: `/api/feedback` POST route, `FeedbackModal` in `MobileSession.tsx`, `feedbackHint` + `showFeedbackModal` state and prop plumbing through `useChat` → `MainApp` → `MobileSession`, the instructional paragraph in `MobileSettings.tsx`. The legacy `feedback` table is retained as read-only history; admin can still view existing entries via `/api/admin/feedback`.
- Manual table rename `manual_components` → `manual_entries` (2026-04-15): Aligns the DB and code with the canonical vocabulary (Manual / Layer / Entry / Checkpoint). Table renamed by hand in the Supabase dashboard; migration `20260415_rename_manual_components_to_entries.sql` codifies the change with idempotent guards. Two RLS policies renamed (`Users can {create,update} own manual components` → `... entries`); third policy already used "manual" alone and was left. Trigger name `update_manual_components_updated_at` left in place — internal label, repoints to the renamed table automatically. All 14 `.from("manual_components")` call sites updated to `manual_entries`. Pure rename — no schema or logic changes.
- Typography minimums + WCAG AA contrast floor (2026-04-15): Darkened `--session-ink-ghost` (#B8B2AC → #756E6A, ~5.5:1 on linen) and `--session-ink-faded` (#8A8480 → #5E5855, ~7:1) so all secondary/tertiary text passes WCAG AA. Added size tokens `--size-meta` (12px), `--size-body` (15px), `--size-prose` (17px), `--size-heading` (22px). `docs/rules.md` typography table raised — mono 12–13px (was 7–9px), sans 14–15px (was 13px), serif 17–22px. 12px is now the stated minimum text size anywhere in the product. `--session-ink-whisper` marked decorative-only. Inline-styles-only rule nudged toward size tokens.
- Sub-12px fontSize migration (2026-04-15): 88 inline `fontSize` values across 16 files (admin pages, admin panels, MobileSession, MobileSettings, MobileNav, SessionDrawer, SettingsRow, BetaFeedbackButton, etc.) bumped from 7–11px to `var(--size-meta)` (12px). Brings all text up to the new minimum stated in `docs/rules.md`. One unicode arrow glyph (`▸`/`▾` in MobileSettings) intentionally preserved at original size — not text.
- Admin docs page (2026-04-15): `/admin/docs` renders the five canonical docs (intent, system, rules, state, decisions) from disk via `/api/admin/docs` and `/api/admin/docs/[name]` (admin-guarded, node runtime, `fs.readFile` + `fs.stat`). Left sidebar ordered intent → system → rules → state → decisions with last-modified date; main panel markdown-renders with `react-markdown` + `remark-gfm`; staleness banner on state.md when mtime > 7 days. Extra "docs mapping" entry embeds `public/admin/docs-mapping.html` via iframe. Admin rail gains a "Docs" entry linking from `/admin`. Read-only — no editing, search, or versioning.
- intent.md canonical rewrite (2026-04-15): Replaced with April 2026 product strategy (hypothesis, how it works, Manual structure, beta scope w/ 2026-04-28 ship, WS1–WS7 workstreams). CLAUDE.md Documentation section now has per-doc one-line descriptions; added hard rules for browser test login (`devtest@test.com` / `testtest`) and the never-run-`npm run build`-while-preview-dev-server-is-running cache corruption.
- Wordmark "MYWALNUT" → "my walnut" (2026-04-15): Lowercase, letter-spacing 15px → 4px, removed `textTransform: uppercase` across MobileSession, MobileManual, reset-password, waitlist, EntryScreen, InfoScreens, LoginScreen, SeedScreen.
- Admin dashboard → `/admin` route (2026-04-14): Promoted admin out of the mobile settings overlay into a dedicated desktop-first route. Persistent left rail (Users / Beta / Feedback) with URL-driven section via `?section=`. Users section is master-detail: 320px user list + inline `UserProfilePane` with sessions/manual/feedback tabs — no more page-jumping when selecting a user. Beta section merges waitlist + allowlist under sub-tabs. Feedback section combines beta feedback (with unread badge on rail) and in-app feedback. Responsive: rail collapses to a horizontal tab strip under 720px. Internals: new `useAdminData` hook centralizes state + fetchers; extracted `UserProfilePane`, `ExtractionPanel`, `AdminManualView`; `src/components/mobile/admin/` → `src/components/admin/`; deleted the 1432-line `src/components/mobile/AdminView.tsx` overlay. `MobileSettings` now links to `/admin` instead of embedding the view.
- Persona rename Sage → Jove (2026-04-14): `PERSONA_NAME` / `PERSONA_NAME_FORMAL` flipped in `src/lib/persona/config.ts`. Abstraction held — system prompt, summary transcript labels, error strings, checkpoint prompts, simulation prompts, SMS templating, and group-gate direct-address regex all pick up the new name automatically. Hand-touched untemplated literals: `public/persona-contact.vcf` FN field, `public/offline.html` brand copy, `public/narrative/*.html` (4 investor/demo pages — visible text flipped, CSS class tokens like `.bubble-sage` / `--sage` preserved). Value-bound test assertions in `system-prompt.test.ts` and `generate-summary.test.ts` updated. `group-gate.ts` keeps `|sage` alternation as a transition fallback so old iMessage groups using "sage" as direct address still route correctly. `APP_VERSION` 2.10.0 → 2.11.0, `PERSONA_VERSION` 2.4.0 → 2.5.0 (Anthropic prompt cache invalidates on first call after deploy). Deferred: `sage_brief` JSONB key in extraction state — needs its own dual-read migration.
- Narrative pages brand fix (2026-04-14): `public/narrative/mantle_intro.html` → `mywalnut_intro.html`, access code `understandingwmantle26` → `understandingwmywalnut26`, 15 Mantle brand references flipped across `index.html` and the intro page. Old preview links and old access code will 404 / reject (small audience, acceptable break).

**Working end-to-end:**
- Auth: magic link + Google OAuth, middleware redirect, session refresh
- Onboarding: pre-auth flow (entry → info screens → seed → anonymous auth), dissolve transition into chat, skip for returning users
- Streaming chat with Jove: SSE, batch rendering, retry on error
- Sliding window: first 2 + last 48 when > 50 messages
- Checkpoint pipeline: Haiku classifier → server-side Sonnet composition (`composeManualEntry`). No inline sentinel — composition always runs server-side after classifier flags a checkpoint.
- Checkpoint cards: inline confirm/reject/refine UI
- Manual building: upsert to `manual_entries`, manual tab with markdown rendering
- Bottom nav: 3 tabs (session, manual, settings), hides on keyboard open
- Session summary: Haiku, fire-and-forget on stale sessions (> 30 min)
- Session history: side drawer, browse/switch past sessions, start new session
- Dev simulate: Settings → "Simulate user" → auto-runs conversation until checkpoint. Persona can emit `[END]` to stop naturally.
- Dev reset: deletes all user data (not profile/auth) + localStorage clear
- Crisis protocol: detects crisis language, provides 988 + Crisis Text Line
- Voice input (Deepgram): auto-scroll during transcription, 3.5-line max height
- Guest-to-real auth conversion after first checkpoint
- Admin panel: user list (sorted by `last_active`), conversation/message viewer with extraction state, access logging. Tab bar: Users / Waitlist / Allowlist / Feedback.
- Exploration mode: "Explore with Jove" from manual entries
- Transcript recognition: regex detects pasted transcripts (iMessage, email, journal, timestamped chat), Jove asks for context and cross-references manual.
- Resonant content: URL detection + fetch, Jove asks what resonated. Hard rule prevents fabricating descriptions when fetch fails.
- PWA: web app manifest, icons, standalone display, service worker (offline fallback, stale-while-revalidate for static assets, network-only for API), update prompt.
- Typography system (2026-03-31): Jove messages use Source Serif 4 16px, user messages DM Sans 15.5px. Jove bubbles with sage-tint background, user messages plain text.
- Session opening states (2026-03-31): First-time welcome with chips, returning user prompt, explore mode, existing session resume. `firstSessionCompleted` localStorage flag.
- Sign-in nudge (2026-03-31): Inline banner for anonymous users after 5+ messages, 24-hour dismiss cooldown.
- Manual share & export (2026-03-31): PDF generation (jspdf) + native share sheet. Falls back to direct download. Gating: < 3 entries shows soft nudge.
- Manual page (2026-04-07, updated 2026-04-09): Flat section headers (name + "N entries" count + hairline rule). Entries as thread cards (line-clamp 3, expand inline, "Explore further with Jove" link). Empty sections show only muted title + "0 entries" + divider. First-visit intro modal (gated by `mywalnut_manual_intro_seen` localStorage) explains the manual and offers "Talk to Jove" or "Got it." Share CTA visible only when entries exist.
- Text Jove via Linq (2026-03-31): Full texting integration replacing Twilio. Shared `persona-pipeline.ts` ensures zero drift between web and text. Checkpoint flow works via text (YES/NOT QUITE/NO). STOP/START/HELP keywords, rate limiting, cross-channel continuity (TEXT badge in web).
- Session drawer text channel (2026-04-01): Aggregated text message view at top of session drawer. Read-only, synthetic "text-channel" ID.
- Narrative presentation (2026-04-02): Static HTML pages at /narrative/ for investor/demo storytelling. Password-gated, five standalone files.
- Group chat Jove (2026-04-01, gate 2026-04-03): Jove detects iMessage groups via Linq webhooks. Facilitator mode (short, no advice, references manual). Scoring-based message gate (emotion/bid keywords, message length, cooldown). Participant removal handling, inactive reminders. Single-mywalnut-user groups only.
- ND pivot (2026-04-06, 4 PRs): Layer rename to autism-specific (SSOT at `src/lib/manual/layers.ts`). Jove voice rewrite (`voice-autistic.ts`: somatic-first, sensory-verbatim, clinical framework ban). Checkpoint composition requires somatic anchor. All onboarding/legal/marketing copy rewritten for ND audience. Unified first-message handling (legacy Path A/B/C dropped). Quality framework rewrite for autistic-mode audit. `profiles.sage_mode` column for future voice modes.
- Legal page updates (2026-04-06): Privacy Policy + Terms of Service updated — provider-agnostic SMS, "not used to train AI" line, sharing sections, non-clinical/non-accommodation disclaimer.
- Closed-beta entry + onboarding gate (2026-04-08): Entry screen shows "Log in" + "Join the waitlist." Post-login onboarding for new users (`profiles.onboarding_completed_at`). Fails open on API error. Migration: `20260408_add_onboarding_completed_at.sql`.
- Beta access system (2026-04-08, admin add-to-beta 2026-04-09, allowlist tab 2026-04-14): Allowlist-gated signup (email + Google OAuth, fails closed). Waitlist page + `/api/waitlist` (rate-limited, 3/hr/IP). Admin: add-to-beta form + per-row buttons on Waitlist tab, status changes, feedback tab with unread count, dedicated Allowlist tab that lists/removes beta_allowlist entries via `GET`/`DELETE /api/admin/beta-allowlist`. Persistent `BetaFeedbackButton` for all logged-in users. Tables: `beta_allowlist`, `waitlist`, `beta_feedback`.
- Pattern feature removal (2026-04-07): Single entry type per layer. No mode flip, recurrence gate, chain walk, saturation, or per-layer cap. `manual_components.type` column dropped (table later renamed to `manual_entries` on 2026-04-15).
- Jove prompt hardening (2026-04-06): Moved sensitive logic out of system prompt into server-side code. `validateMaterialQuality` pre-emit gate, `validateComposedEntry` post-validator. ~75 lines of enforcement scaffolding pruned from prompt.
- Security hardening (2026-04-07): Upstash rate limiting on chat/summary/checkpoint/OTP routes (fails open if env vars missing). Anon Gate B at 2 manual_entries. OTP phone verification (SHA-256, 6-digit, 10-min TTL). PII logging cleanup.
- Jove prompt tuning (7 rounds, 2026-03-17 through 2026-04-07): Abstract stacking rule, receive-land-ask rhythm, checkpoint delivery sequence (verbatim body/sensory word required, open validation question, 4-8 word headline), post-checkpoint advisory mode, clinical-label-in-negation rule, one-question-per-turn enforcement, therapy-ism bans ("sit with," "lean into," "hold space for").
- Sage → Persona rename (2026-04-14): Internal identifier rename across ~70 files. User-facing "Sage" strings routed through `PERSONA_NAME` constant at `src/lib/persona/config.ts`. Directory moved `src/lib/sage/` → `src/lib/persona/`, files renamed (`call-sage.ts` → `call-persona.ts`, `sage-pipeline.ts` → `persona-pipeline.ts`, `sage-bridge.ts` → `persona-bridge.ts`, `ExploreWithSageButton.tsx` → `ExploreWithPersonaButton.tsx`). Contact card `public/sage-contact.vcf` → `persona-contact.vcf`. DB migration `20260414_rename_sage_to_persona.sql` renames `sage_*` columns (e.g. `profiles.sage_mode` → `profiles.persona_mode`). System prompt rewritten in second-person with `{{PERSONA_NAME}}` templating. Direct-address detection routed through `mentionsPersona` helper.
- Brand migration Mantle → mywalnut (2026-04-09): 52 files across app, API, Jove prompt, Linq SMS, legal pages, onboarding, PWA manifest, service worker, scripts, docs. DB column `mantle_user_id` → `owner_user_id` (migration `20260409_rename_mantle_user_id.sql`). localStorage keys `mantle_*` → `mw_*` with one-time migration shim. Domain `mywalnut.app`. External steps: Supabase redirect URLs, Linq webhook re-registration, contact card update.

## Not Yet Functional
*Last verified: 2026-05-07*

- **Multi-user group conversations**: Groups with 2+ mywalnut users get neutral intro but can't create conversation records (user_id NOT NULL constraint). Facilitator mode only works for single-mywalnut-user groups.

## Known Issues
*Last verified: 2026-05-14*

- **Auth token expiry**: No explicit token refresh on the client. Relies on middleware `getUser()` per page request. Long SPA sessions could expire. API routes return 401 → redirect to /login.
- **Ghost conversation rows**: Race condition if user sends a message before conversationId state updates. Mitigated by ref guard but not impossible.
- **OAuth redirect config**: Supabase dashboard Redirect URLs allowlist must contain callback URL for every environment. Defensive `?code=` forward in middleware recovers from allowlist drift.
- **Waitlist rate limiter not enforcing**: Upstash env vars (`UPSTASH_REDIS_REST_URL`, `UPSTASH_REDIS_REST_TOKEN`) are not set in production. Rate limiter fails open by design. Code is wired correctly — just needs the Redis instance.

## In-Flight Work
*Last verified: 2026-05-12*

- Dark-mode redesign is fully shipped — all 9 phases plus follow-ups. See the Deployed Features entry.
- Light-mode redesign is fully shipped — all 8 phases. See the Deployed Features entry.
- **Checkpoint hardening plan** ([docs/checkpoint-hardening-plan.md](checkpoint-hardening-plan.md)) — 6-track plan to harden the checkpoint system for scale before 2026-04-28 beta. Tracks: (1) Supabase CLI-driven migrations + drift detection, (2) idempotent confirm RPC + unique constraint + transactional writes, (3) client-side resilience with error taxonomy and SW audit, (4) structured logging + confirm_failures table + admin health panel, (5) E2E integration test + schema drift canary, (6) Upstash Redis + enforced rate limits. Pre-beta cut: Tracks 1, 2, 3, 6 (~4 days). Plan includes pre-flight SQL audits, concrete RPC + log schemas, per-track rollback plans, and an explicit decisions table to avoid mid-execution litigation.
- PWA Phase 3 pending: standalone polish, auth flow testing, splash screens — needs device QA
- Beta recruitment: target 10 late-diagnosed autistic adults, ages 25-45
- **🚨 Beta-blocking — Upstash env vars missing in Vercel production.** `UPSTASH_REDIS_REST_URL` + `UPSTASH_REDIS_REST_TOKEN` are unset; all rate limiters fail open in prod per runtime logs. After ADR-038 removed the per-row `otp_attempts` counter (2026-04-16), Upstash is the **only** remaining abuse-protection layer on the OTP send + verify endpoints. Until these env vars are set and redeploy lands, an attacker with a valid phone number can attempt unlimited verify-code guesses within the 10-minute TTL. Fix: provision Upstash Redis REST, add both env vars to Vercel (Production + Preview + Development), redeploy. See ADR-038 for the rationale + bounds analysis. Do not open beta enrollment until this is closed.
- Linq deprecation: dual-provider architecture shipped 2026-04-16 — 1:1 text on Sendblue, group facilitator on Linq. Future work: port group facilitator once participant-diff logic is rebuilt around per-message `participants[]` arrays. See ADR-035.

## Beta Users
*Last verified: 2026-03-15*

- [Jeff to fill: current beta user count, any active testers, recruitment status]
- Target: 10 late-diagnosed autistic adults, ages 25 to 45 (often AuDHD). Audience defined by demographic and lived experience, not by recruitment channel.
- Success metric: 3+ out of 10 return for 3rd session unprompted within 2 weeks

## Test Suite
*Last verified: 2026-05-14*

- Test count: 730
- All pass, ~1s, zero API cost (all mocked)
- Framework: Vitest with vite-tsconfig-paths
- Run: `npm run test` (all) or `npm run test:watch` (dev mode)
